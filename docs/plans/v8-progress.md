# RACE2 v8 — Progress Log

## Phase 1 — Camera suite (2026-09-14, commit `v8 Phase 1`)

**Shipped**
- `CameraRig` 3 modes (src/render/camera.ts): CHASE (v2.2 behavior, byte-identical — proven by a legacy-rig oracle over a full scripted lap, max pos err < 1e-9), CLOSE (follow 8.8→5.7 = −35%, height 3.1→1.9 = −39%, look-ahead 7→8; lookBack 8.4→5.5 / −14→−9), HOOD (eye = pos + up·1.05 + fwd·0.5, look-ahead 10→12.5 = +25%, FOV base forced 70). Mode transitions are instant (snap-on-change, no eased swings).
- Hood POV hides `carVisual.bodyGroup` only — wheels + contact shadow stay; restored automatically in photo/podium/menu/replay states (main.ts per-frame `pov` check).
- Settings: `settings.cam` ('chase'|'close'|'hood', default chase) + `settings.fov` (60–100, default 72) + `settings.hintCam`. Legacy v2.2 `settings.camera` ('chase'|'hood') migrates into `cam` on load; invalid values sanitized, fov clamped. Wired live-applied via onSettingsChanged (the old `camera` select was dead — it never reached the rig; now it does).
- FOV mapping: slider value is the stored preference; CHASE/CLOSE base = fov − 10 (default 72 → base 62 = today's chase, satisfying the byte-identical hard constraint), HOOD base forced 70. Speed-FOV response shapes unchanged on top of the base (chase/close: speedRatio·22 + eased 4° + kick; hood: speedRatio·16 + eased 4°).
- C key (and gamepad RB / touch camera button) cycles CHASE→CLOSE→HOOD during racing, persists to settings, one-time "C — CAMERA" HUD hint (fires at race start only when no mode hint claims the slot). Photo mode and menu orbit/replay cameras untouched.
- Hood speed lines intensify: onset 0.62→0.45 speed ratio, cap 0.85→1.0.
- QA hook: `__race2.cam()` get / `__race2.cam('chase'|'close'|'hood')` set (persists).

**Tests**
- New permanent gate `test/camera.ts` (28 checks): cycle order, FOV base math per mode/pref, CHASE byte-identity vs inline v2.2 oracle over a full lap (1e-9), per-mode scripted laps (no NaNs every step; car-in-frustum 100% chase/close; ahead-facing 100% hood; hood fov ≥ 70), mid-lap cycling (6 swaps, no NaNs, stays framed). Exit 1 on failure.
- `test/allocs.ts` now runs the rig (all 3 modes cycled) inside the probe — PASS, +0.000MB/30s gc-forced.

**Gates (all green)**: typecheck ✓ build ✓ laps 14/14 rivals 20/20 career 119/119 share 34/34 knockout 50/50 daily 59/59 probe 24/24 allocs PASS camera 28/28.

**Evidence** (qa/v8-phase1/, muted, not committed): alpine-day-{chase,close,hood}, neon-night-{chase,close,hood}, fov-60 vs fov-100 (live fov 64 vs 104 at same corner/speed), ckey-1/2/3 (real keypress cycle), settings-panel{,-changed} (CAMERA select + FOV slider live-apply), persistence reload check (cam=close fov=88 → reload → rig applies both).

**Verdicts (lead-reviewed; no subagent tool in this session)** — CHASE: readable, unchanged ✓. CLOSE: readable + more immersive, car fills more frame, curbs/braking marks clearer — YES. HOOD: usable at 137–144 km/h, road well centered (look-ahead +25% works), body hidden with front wheel hubs grounding the view; night variant slightly dark but headlights carry it — YES, with a nit: a close ghost can occlude the view (same as any chase view).

**Deviations / notes**
1. FOV slider semantic: task said "slider value is the FOV base" (default 72) AND "chase byte-identical when settings untouched". Both can only hold if the base is offset: CHASE/CLOSE base = slider − 10. Documented in save.ts + camera.ts comments; one-line change if literal mapping is preferred instead.
2. `settings.camera` key replaced by `settings.cam` (per spec) with one-time migration from the old key; old JSON key is deleted on load. No data loss.
3. Screenshot judging done by the lead agent (no subagent dispatch available in this environment); all shots are real muted gameplay at 97–144 km/h.

## Phase 2 — Ghost battle (2026-09-14, commit `v8 Phase 2`)

**Shipped**
- `race.ts` refactor: the single-ghost fields (`ghost`/`ghostDists`/`ghostCpSplits`) became `GhostTrack[]` (`{samples, dists, cpSplits, label, color}`), built per-ghost by `buildGhostTrack()` (surfaceQuery dist precompute + checkpoint split scan, previously done inline in `start()`). Public API now indexed: `ghostCount()`, `ghostTrack(i)`, `ghostTracks()`, `ghostSampleAt(t, i)` (+ allocation-free `ghostSampleInto(t, i, posOut, quatOut)`), `ghostDistAt(t, i)`, `liveGhostDelta(dist, t, i)`, `ghostSplitDelta(ghostIndex, cpIndex)`, and new `battleRank(dist, t)` → `{rank, of}` (player rank among player+ghosts by progress at that instant). Checkpoint-event split deltas and finish-panel split deltas still use the primary ghost (index 0). `ghostActive` = any ghost present. Behavior with a single active ghost is byte-equivalent to v2.2.
- Slot filling (`loadGhostTracks`): candidates in order FRIEND (external/imported ghost — keeps v2.2 semantics where an imported friend ghost is THE ghost), PB (`trackSave.ghost`), BOT (`DEV_GHOSTS`); fill up to `maxGhosts` (settings.ghosts, clamped 1–3). Dedupe per spec: dev is skipped when a PB occupies a slot and the limit leaves no room (limit 2 + PB + friend → FRIEND+PB; limit 2 + PB → PB+BOT; no PB → FRIEND+BOT; default 3 → FRIEND+PB+BOT). `writesRecords=false` (rival/knockout/daily/traffic-era modes) loads zero ghosts — suppression unchanged. Deviation from the task's literal priority order (PB first): FRIEND takes the primary slot so GHOSTS=1 in a friend race still shows the friend ghost and checkpoint deltas compare against it, preserving the v2.2 friend-race contract; noted as intentional.
- Finish: `FinishResult.ghostResults[]` (`{label, color, deltaMs}`) where `deltaMs = player − ghostLapMs` (negative = beat). PB submission/medal/records untouched; `#g=` import flow untouched; SOCIAL CLIMBER wiring (`friendRaceActive` → `addStats({friendGhostRaces:1})`) intact and now also fires when the friend ghost races alongside PB/BOT.
- Friend ghosts now survive reloads: `decodeSavedFriendGhosts()` decodes persisted `save.friendGhosts[*].code` at boot into an in-memory per-track cache (async deflate; failures silently ignored). `raceFriendGhost`/`startTrack` read cache with fallback to the legacy in-session import.
- HUD: per-ghost delta chips (stacked `.hud-ghost-delta` rows, label colored per `GHOST_COLORS` — PB white, FRIEND accent-2 #ffb52e, BOT grey; value green/red ahead/behind; hidden in drift mode like the old live delta); battle-rank chip `.hud-battle-rank` ("2ND OF 4" ordinals, hidden when <2 racers or rival mode); progress bar now renders up to 3 per-ghost dots. Old single `hud-live-delta` element kept (always null in time-trial now — chips replace it). Minimap `update()` takes `{x,z,color}[]` ghost dots (preallocated buffer + length-mutated view, no per-frame allocs).
- Car rendering: 3 ghost visuals (one per slot) built per track; paint per label (PB = translucent player paint as today, FRIEND = #ffb52e, BOT = #8d96a5) via existing `CarVisual.setPaint`; garage paint changes live-update all slots.
- Settings: `settings.ghosts` (1/2/3, default 3, sanitized in `loadSettings`) + "GHOSTS" select in Settings (time-trial only — rival/knockout never load ghosts regardless). `race.maxGhosts` set from settings at startTrack; changing mid-race applies next race.
- QA hook: `__race2.ghosts()` → `{active, maxGhosts, settings, tracks:[{index,label,color,samples,lapMs}]}`.

**Tests**
- New permanent gate `test/ghostbattle.ts` — 39 checks, exit 1 on failure: slot filling/dedupe matrix for limits 1/2/3 × PB/friend/dev availability + rival-suppression; per-ghost sample lookup at t=0/mid/past-end + out-of-range index; battleRank ahead/midfield/behind mixtures + of-count + boundary stability; per-ghost live deltas (≈0 at the ghost's own position, sign convention vs slower ghost, distinct per ghost, null outside range); a real autopilot finish on sunrise-sprint with 3 ghosts asserting the emitted `ghostResults` rows (labels, colors, delta math vs synthetic lap times); allocs probe (30s 3-ghost sim with per-step `battleRank`/`liveGhostDelta`/`ghostDistAt` + render-pattern `ghostSampleInto`, gc-forced, +0.30MB ≤ 2MB PASS). Run `npx tsx --expose-gc test/ghostbattle.ts`.

**Gates (all green)**: typecheck ✓ build ✓ laps 14/14 rivals 20/20 career 119/119 share 34/34 knockout 50/50 daily 59/59 probe 24/24 allocs PASS camera 28/28 ghostbattle 39/39.

**Evidence** (qa/v8-phase2/, muted `?mute=1`, real races, not committed): seeded synthetic PB (1.06–1.22× dev pace) + imported friend code + real dev ghost on sunrise-sprint and salt-flats (`?alltracks`). battle-midrace.png (sunrise: FRIEND −6.53 / PB −3.17 / BOT +0.05 chips, FRIEND pill, "2ND OF 4", 3 progress dots + minimap dots); battle-3cars.png (salt-flats, 4TH OF 4 with all three ghosts ahead +2.4/+1.6/+1.7s); battle-midrace-closeup.png (ghost chain ~30m ahead visible); battle-finish.png (GHOST BATTLE rows: FRIEND BEAT BY 7.77s, PB BEAT BY 3.76s, BOT LOST BY 0.08s — math verified vs seeded lap times, and a second run on salt-flats: LOST BY 3.37/1.66/1.82s); battle-ghosts1.png (GHOSTS=1: single FRIEND chip, "2ND OF 2", one ghost car visible); settings-ghosts.png (GHOSTS select = 3); render-probe.png (frozen-ghost render check).

**Verdicts (lead-reviewed, honest)** — HUD: chips/rank/dots all legible at speed, color-coding reads instantly, rank chip is a genuine race-feel addition ✓. Finish rows: exactly the right "beat/lost by" summary, correct signs and colors ✓. GHOSTS=1 vs 3 comparison: clean behavioral difference (chips count, rank denominator, dot count) ✓. Ghost CARS: render correctly (render-probe) but in real racing they follow the same recorded line time-shifted, so at typical gaps they read as a tight chain/one blob at distance, and they're occluded by crests/bends like any car; on flats with 15–40m gaps the chain is clearly visible. Acceptable for v8; a future polish could add per-ghost lateral offsets or larger ghost labels above cars.

**Deviations / notes**
1. FRIEND outranks PB in slot order (see above) — deliberate, preserves friend-race semantics; default-3 line-up matches the spec (PB + dev + friend; no-PB → dev + friend).
2. Settings default is 3 per the task spec (roadmap said default 2 — task supersedes).
3. Live delta chips vs BOT/PB become `null` (chip hides) when the player's dist is outside a ghost's sampled range (lap wrap vicinity) — pre-existing liveGhostDelta boundary behavior, now per-ghost.
4. `useExternalGhost` kept as the FRIEND-slot feed (main.ts call site unchanged in name); it now coexists with PB/BOT instead of replacing them.
5. Pre-existing bug found (out of scope, unfixed): controls help advertises "Restart — R" but `input.restart` is never consumed in main.ts; restart works only via pause menu. QA restarts were done by reloading the page.
6. Screenshot evidence methodology: dev ghosts and the autopilot share one racing line, so staged evidence used synthetic PB/FRIEND paces (generated from DEV_GHOSTS via serialize/encode) seeded into localStorage plus scripted throttle control; all screenshots are real muted gameplay.

## Phase 3 — Traffic rush (2026-09-14, commit `v8 Phase 3`)

**Shipped**
- `TrafficManager` (src/systems/traffic.ts): 8-12 kinematic traffic cars per track, deterministic via `mulberry32(hashSeed('traffic:' + trackId))` — same layout every race on a track. Each car is `{dist, lane, speed, paint}`; stepping is `dist += v·dt` (wrapped) along a fixed lane, NO CarPhysics. Pace = 40 m/s reference × 55-70% band, low-biased (rng²) so closing speeds clear the 12 m/s near-miss bar. Lanes alternate left/right, edge-biased 1.8-3.2 m (keeps the center racing line open for squeezes). Loop-respawn: any car >80 m behind the player re-spawns +120 m ahead → count constant (asserted in test). Player↔traffic contact band |Δdist| < 3.2 && |Δlat| < 1.7 → strong scrub on the player only (`vel × (1−6·dt)` per step, stronger than the 3.5 mover scrub) + once per 0.8 s cooldown per car: spark burst at contact midpoint, `audio.crash()` thud hook (P7 replaces with a dedicated thud), rumble + shake, traffic car nudged ±2 m laterally for ~1 s then smoothstep-eased back (clamped to road half-width). NO physics damage, NO race end, NO traffic slowdown (kinematic).
- Near miss: per-car pass state machine — on ahead→behind crossing (|rel| < 10 m guards against respawn/teleport false-positives) a ±0.4 s window opens; if min|Δlat| < 2.2 m, closing speed > 12 m/s, and no collision inside the window → NEAR MISS chip (center flash) + HUD counter bump. Max 5 credited per race (counter shows raw count with 5+ display cap; finish panel credits `min(n, 5)`); applied ONLY at the finish panel — `elapsedMs` untouched mid-race.
- TRAFFIC mode chip (menus.ts) joins DRIFT/RIVALS/KNOCKOUT, fully exclusive; career/daily/friend flows force-clear it; `startTrack` guard `careerRace/dailyRace === null` mirrors knockout. Traffic race = 1 lap, time-trial checkpoint/finish rules (92% anti-cheat intact), `writesRecords = false`, ghosts forced 0 (no PB/ghost/drift/lifetime writes anywhere in the finish path). Finish panel: raw time, `NEAR MISSES n (c CREDITED) · BONUS −X.XXs`, `SCORE` (bonus-adjusted), per-track best line or `🏆 NEW TRAFFIC BEST` banner; medal/splits/history/PB rows and WATCH REPLAY/SHARE GHOST buttons suppressed; RETRY / NEXT TRACK (stays in traffic mode) / TRACK SELECT remain.
- Save: additive `AllSaves.trafficBest: Record<trackId, scoreMs>` + `recordTrafficBest()` + `sanitizeTrafficBest()` (default-merge, never wipes existing keys). Stores the bonus-adjusted score (roadmap's "score" semantics).
- HUD: `hud-nearmiss` counter in the top bar (traffic races only) + `splash-nearmiss` center flash (accent-2, 1.4 s); one-time `hintTraffic` context hint ("TRAFFIC AHEAD — THREAD IT…") via the existing hint plumbing (`settings.hintTraffic` additive).
- Traffic rendering: exactly 3 draw calls — two InstancedMeshes (painted body w/ per-instance muted civilian colors; trim/glass/wheels baked as one vertex-colored geometry) + one instanced contact-blob plane. Geometry baked once per style in car-model.ts (`trafficCarGeometry`, shares the car-model part set/cache, `userData.shared` so rebuilds don't dispose it); frustum culling off; no shadow casting, no boost flames, static wheels (invisible at speed).
- QA hooks: `__race2.start(i, 'traffic')`, `__race2.traffic()` → `{mode, count, nearMisses, credited, collisions, cars[]}`.

**Tests**
- New permanent gate `test/traffic.ts` — 16 checks, exit 1 on failure: 1-lap autopilot traffic races on sunrise-sprint (10 cars) + dune-rush (8 cars) both finish; ≥1 natural near-miss on each (2+2 observed); scripted collision (slow cork dropped on the player's line at 30+ m/s) → contact registered, no NaN, player recovers 28.6 → 34.6 m/s and stays mobile; traffic count constant across loop-respawn; bonus/credit panel math; accumulator step integrity vs a no-traffic baseline (Δ11 steps ≤ 600 tol).
- `test/allocs.ts` probe extended with `traffic.update` + `traffic.updateVisuals` (warm-up + both measured windows) — PASS, +0.000 MB/30 s gc-forced.

**Gates (all green)**: typecheck ✓ build ✓ laps 14/14 rivals 20/20 career 119/119 share 34/34 knockout 50/50 daily 59/59 probe 24/24 allocs PASS camera 28/28 ghostbattle 39/39 traffic 16/16.

**Evidence** (qa/v8-phase3/, muted `?mute=1`, real races, not committed): sunrise-sprint (alpine): sunrise-pack (start), sunrise-nearmiss (NEAR MISS flash + counter mid-pass at 142 km/h), sunrise-collision (spark burst at contact, nudged tan car easing back, pack ahead), sunrise-finish (panel: 17.633 − 0.30 s = SCORE 17.333, 🏆 NEW TRAFFIC BEST). neon-circuit (neon night): neon-pack/neon-nearmiss (threading a 2-car pack, chip live, third car beside camera), neon-sparks (sparks + dense 6-car pack at 135 km/h), neon-finish2 (panel: NEAR MISSES 2 · BONUS −0.30s, SCORE 14.967, "Traffic best: 14.942" — correct no-banner path because the earlier run held the best). traffic-chip (track-select header with TRAFFIC chip on). Persistence: page reload → `localStorage race2.save.v1.trafficBest = {sunrise-sprint: 17267, neon-circuit: 14942}` intact.

**Perf**: traffic mode census on neon-circuit = 76 draw calls / 58.7k tris (budget ≤220; traffic adds exactly 3).

**Verdicts (lead-reviewed, honest; no subagent tool in this environment, same as P1/P2)** — Readability: YES, clearly — muted blue/grey/tan civilian paints separate traffic from the neon player car in both themes, contact blobs ground them at night, and the pack reads at 100+ m. Near-miss moment: YES — chip + counter + pass geometry makes overtakes feel earned; 2-3 natural near misses per lap on the probed tracks. Collision: YES — sparks + thud + sideways nudge reads instantly, recovery is fast (test: min 28.6 m/s), and the 0.8 s cooldown prevents grind-lock; fair (no damage, no DNF). Fun: YES with a note — the autopilot (which cannot dodge) eats 2-5 contacts/lap, so a human weaving deliberately has real agency; density (8-12/lap) sits on the sparse side of the P9 target (3-8 near misses per run), which is the right side to err on for a 1-lap sprint. Honest nit: traffic cars ignore the player entirely (per spec kinematics), so cars never react — fine for arcade dodging, revisit only if P9 balance says otherwise.

**Deviations / notes**
1. Collision lateral band is 1.7 m, not the task's 2.2 m — with a 2.2 m contact band a near miss (<2.2 m) is geometrically unreachable because every pass crosses Δdist ≈ 0. The 1.7 m car-width contact band + 2.2 m near-miss band creates the intended clean-squeeze corridor (documented in traffic.ts). Dist band stays 3.2 m.
2. Traffic pace is low-biased inside the 55-70% band (rng², effective ~55-62%): a uniform band makes closing speeds fall under the 12 m/s near-miss threshold vs a ~35-40 m/s player. Roadmap P9 balance pass may revisit.
3. Lanes alternate sides (spec) but are edge-biased 1.8-3.2 m instead of hugging center — center lanes put every car on the autopilot racing line and produce all collisions / zero near misses.
4. `trafficBest` stores the bonus-adjusted score (the roadmap's finish-panel "score"), not the raw time.
5. WATCH REPLAY is hidden in traffic because `writesRecords=false` records no replay samples (same suppression path as rival modes) — consistent, not a regression.
6. All 13 gates re-run green after the final tuning pass; steering-direction convention untouched (no car.ts/curve.ts changes — traffic orients via the podium-proven `(normal×tangent, normal, tangent)` basis).

## Phase 4 — Weekly event (2026-09-15, commit `v8 Phase 4`)

**Shipped**
- `src/game/weekly.ts` (new): `weekKeyFor(date)` = UTC ISO week `YYYYWnn` (Monday-based, week 1 contains the first Thursday; verified across 53-week 2020 and the 2025/2026 boundary) + `isValidWeekKey`/`prevWeekKey`/`weekIsLive` (round-trip through Monday recomputation, so impossible weeks like 2027W53 are rejected). Seed = FNV-1a('race2-weekly:' + weekKey) → mulberry32 shuffle of the 14 tracks → 3 distinct tracks on 3 distinct themes (fallback fill if only 2 themes remain in the shuffle tail); lineup per race = `pickLineup(trackId, raceIndex+1, ['mid','mid','pro'])` (roster-capacity-guarded: 3 mid roster / 2 pro roster). 2 laps per race, cup points 25/18/15/12 (reuses career `CUP_POINTS`).
- Modifier rotation: `WEEKLY_MODIFIERS` 4 slots indexed by `floor(mondayUtcMs / 604800000) % 4` — a continuous epoch-week counter, so 4 consecutive weeks ALWAYS cover all 4 modifiers, including across 53-week year boundaries (2020W52→2021W01 tested). Modifiers: `RAIN FINALS` (races 2-3), `NIGHT OWL` (all), `SLICK MAYHEM` (slick rects ×2 l+w), `BOOST FEST` (pad strength ×1.5).
- Modifier plumbing (no physics changes): rain/night flow through `weeklyVariant()` → the existing variant path (`resolveVariant` weekly branch → `surfaceGripFor(VARIANTS[v].rain)` for player, `rivals.rain` for AI, night headlights via environment rig). Slick/pad modifiers flow through `weeklyRaceTrack()` which returns a runtime COPY with scaled rects — the shared TRACKS defs are never mutated (asserted by JSON snapshot + array identity in tests); weather modifiers return the shared def object unchanged.
- Flow (mirrors daily/career): title WEEKLY button with streak pill → weekly card (week header, modifier badge, 3 track chips, unique roster paints, meta line, per-race R-pills, DONE line, STREAK line, RESUME·RACE x/3 or RE-RACE labels) → races via career-style finish panels (WEEKLY Rx/3 line + WEEKLY STANDINGS table + NEXT RACE / SAVE & QUIT) → `showWeeklyInterstitial` between races (race x/3, track, badge, standings, R-pills, START RACE) → final panel = trophy banner (GOLD/SILVER/BRONZE — WEEKLY, P4 = none) + FINAL WEEKLY STANDINGS + SHARE RESULT + BACK TO TITLE.
- Save (additive default-merge): `AllSaves.weekly: { run: {weekKey, nextRace, entries, positions} | null, best: {[weekKey]: {points, position}}, streak, lastWeek }` + `sanitizeWeekly` (corrupt payloads never throw) + `getWeeklyRun/setWeeklyRun/recordWeeklyFinish` (best = most points then best position; streak +1 when the finished week is exactly the previous ISO week — crosses year boundaries — same-week retries keep it, gaps reset to 1). Weekly races write ONLY this ledger — no PB/ghost/cup/daily/lifetime writes (guarded in the finish chain before the rival-stats branch, like career).
- Share: `#w=<weekKey>.<points>.<position>` (`buildWeeklyLink`/`parseWeeklyLink`, points 0-999, position 1-99) → boot hash check → `showWeeklyImport` (BEAT MY SCORE card, modifier badge, THEIR WEEK line; RACE THIS WEEK'S EVENT enabled only when `weekIsLive`, old weeks show the disabled card) — hash cleared on load like `#d=`.
- Resume mid-week persisted: run survives reloads (Map-backed reload tests + live-verified in browser); stale-week runs are discarded when the week rolls over.
- QA hooks: `__race2.startWeekly()` (starts/resumes the week, returns race summary) and `__race2.weekly()` (def: tracks/lineups/modifier/laps + live race + run + streak/best + lastFinish); `state()` gained `weekly`. Dev seed override `?week=2026Wnn` (validated) forces any week for QA — card, races, and hooks all honor it; mirrors `?variant=`/`?alltracks`.

**Tests**
- New permanent gate `test/weekly.ts` — 116 checks, exit 1 on failure: weekKeyFor correctness (UTC rollover, 53-week leap year, year boundaries, invalid keys), seed determinism (byte-identical defs, 12-week stability, mixed themes, 2-mid+1-pro rosters), 4-consecutive-week rotation coverage incl. year boundary, week liveness, modifier plumbing (variant mapping, RAIN_GRIP_MULT composition, slick doubling on the runtime copy only with a `computeOnSlick` physics probe point, pad ×1.5 with an `updateBoostPads` velocity-kick measurement, shared defs untouched), run/standings/trophy math vs CUP_POINTS, mid-week resume + stale-week discard + streak matrix (incl. 2020W53→2021W01) + best ledger, `#w=` round-trip + rejections + old-week disable, save sanitizer, and a FULL 3-race headless run on the seeded rain-finals week (autopilot + STRICT wedge assist; per-race grip assertions 1 / 0.82 / 0.82; completes with silver trophy; zero PB/ghost/cup/daily/lifetime writes).
- Fixed during QA: `weeklyScreen` was missing from menus `hideAll()` (card stayed overlaid when a race started) — found in live browser testing, added to the list.

**Gates (all green)**: typecheck ✓ build ✓ laps 14/14 rivals 20/20 career 119/119 share 34/34 knockout 50/50 daily 59/59 probe 24/24 allocs PASS camera 28/28 ghostbattle 39/39 traffic 16/16 weekly 116/116.

**Evidence** (qa/v8-phase4/, muted `?mute=1`, real runs): weekly-card-slick-mayhem (current week 2026W38 card: SLICK MAYHEM badge, 3 chips, 5 roster paints); weekly-race1-midrace (canyon-twist, rival HUD + minimap, 134 km/h); weekly-race1-finish (WEEKLY R1/3: P2 · +18 PTS · 18 TOTAL + WEEKLY STANDINGS table); weekly-interstitial-r2 (RACE 2/3 + badge + standings + R1 P2 pill); weekly-race2-midrace / weekly-race3-midrace; weekly-final-standings (GOLD TROPHY — WEEKLY banner, FINAL WEEKLY STANDINGS 54 PTS P1, SHARE RESULT); title-weekly-streak (WEEKLY STREAK 1 pill); weekly-card-completed (DONE: P2 · 54 PTS + STREAK 1 + RE-RACE); weekly-import-current (fresh profile `#w=2026W38.54.2` → BEAT MY SCORE, RACE THIS WEEK'S EVENT enabled); weekly-import-oldweek (`#w=2026W36.40.2` → disabled button + THAT WEEK'S EVENT note); weekly-card-night-owl + weekly-race-night (forced `?week=2026W41` — night variant, headlights, stars); weekly-resume-card (W40 after race-1 win + reload → R1 P1 pill + RESUME · RACE 2/3); weekly-race-rain (race 2 harbor-nine: variant rain, live `surfaceGrip` 0.82, rain streaks).

**Verdicts (lead-reviewed, honest)** — Card/interstitial/finish chain reads exactly like the career flow and the modifier badge makes the week's twist legible before committing ✓. Points/trophy math verified live (P2×3 = 54 pts = silver in headless, gold live after a race-1 win) ✓. Resume across reload works and shows the right race ✓. Old-week share cards disable correctly ✓. Honest nits: (1) `navigator.share` never settles in headless Chrome so the SHARE button stays on "SHARING…" there — real browsers resolve/reject normally, and the daily share shares the same pre-existing pattern; evidence used the clipboard/hash path instead. (2) After playing a SLICK MAYHEM race, the menu orbit background keeps the doubled-slick visuals until another track loads (cosmetic, scene reuse). (3) `?week=` is a dev-only override; production weeks always come from the UTC clock.

**Deviations / notes**
1. Rotation index uses continuous epoch-Monday weeks (not `isoWeek % 4`) — a literal ISO-week modulo repeats a modifier at every 53-week year boundary (53%4==1%4); the task's "4 consecutive weeks → all 4 modifiers" test forced the continuous form.
2. `startTrack` reload condition changed from `def.id !== this.track.id` to `def !== this.track` (object identity) — required so a modifier copy of the track the player last raced actually reloads with doubled slicks/pads; behavior for shared defs is unchanged (TRACKS members are stable objects).
3. Career standings-table title text left byte-identical ('FINAL STANDINGS'); weekly uses its own titles.
4. `stopReplay` now passes `lf.traffic` (and `lf.weekly`) to showFinish — pre-existing latent drop of the traffic panel; unreachable in shipped flows (replay is time-trial only) but fixed for consistency.

## Phase 5 — Paint unlocks + driver stats (2026-09-15, commit `v8 Phase 5`)

**Shipped**
- Paint roster (src/render/car-model.ts): 6 of 10 paints stay free and byte-identical (Cyan Flux 0x29e6ff, Solar 0xffb52e, Rose Rush 0xff4d6d, Volt 0x7dff6e, Frost 0xe8f2ff, Lime Pop 0xc8ff2e — pinned by test); 4 are now earnable via optional `lock?: PaintLockId`: **MIDNIGHT** 0x223055 (`knockout-win` = win any knockout race), **SUNBURST** 0xff5c39 (`daily-streak` = reach a 7-day daily streak), **ULTRAVIOLET** 0xb44dff (`near-miss` = 25 career near misses), **GOLD LEAF** 0xd78a4a (`weekly-gold` = weekly gold trophy). The three renamed locked entries keep their v2.2 colors (color = saved-profile identity), only names changed: Ultra→Ultraviolet, Magma→Sunburst, Copper→Gold Leaf.
- Unlock engine (src/game/unlocks.ts, new): `PAINT_LOCK_INFO` (label + requirement text), `paintUnlockState` (garage display map, mirrors the bodyUnlocks pattern), `paintRequirementMet` (reads `stats.knockoutWins`, `daily.bestStreak/streak >= 7`, `stats.nearMisses >= 25`, `weekly.best[*].position === 1`), `checkPaintUnlocks` (awards every newly-satisfied lock; idempotent per id — repeat calls return empty, no double-toast).
- Persistence (src/core/save.ts, all additive default-merge): `AllSaves.unlocks {paints[]}` + `sanitizeUnlocks` (dedupes, drops unknown ids) + `unlockPaint()/isPaintUnlocked()`; LifetimeStats gains `nearMisses`, `distanceKm`, `knockoutWins`, `rivalWinsBy: Record<rivalName, wins>` (sanitized per-key); DailySave/WeeklySave gain `bestStreak` (monotonic max, updated inside `recordDailyFinish`/`recordWeeklyFinish`, never decreases on streak break).
- Trigger wiring (src/main.ts): knockout win path (`addStats({knockoutWins, rivalWinsBy})`, guarded `!r.knockout`, eliminated rivals excluded from head-to-head since OUT rows are eliminations not finishes), traffic finish (`addStats({nearMisses: raw count})` before the credit cap), rival-mode finish (existing branch now also writes `rivalWinsBy`), daily/weekly finishes write their own ledgers only (lifetime guard untouched) but feed streak/trophy state; `announcePaintUnlocks()` runs after every finish chain AND on boot sweep in the constructor (catches migrated stats, e.g. imported near-miss counts). Toast `NEW PAINT UNLOCKED — <NAME>` + new `audio.unlockChime()` (dedicated 'unlock' blip bus, P7 may revoice).
- Garage UI (src/ui/menus.ts + styles.css): locked swatches wrap in `.paint-cell` with requirement text under the swatch (garage body-lock pattern), dimmed + desaturated, tooltip `<NAME> — <REQ>`; clicking plays a shake (`.deny` keyframes) + toast `LOCKED — <REQ>`; selection gate via `paintSelectState(save, color)` in garage.ts (headless-testable). Free paints' click path unchanged. Grid rows auto-height only when lock labels exist.
- Stats page (src/game/stats.ts new `driverStats()` single source of truth + menus `buildStats()`): title-nav STATS button (between GARAGE and ACHIEVEMENTS), sections CAREER (laps, distance km, drift points, air time, near misses), RACING (rival wins, knockout wins), TROPHIES (gold/silver/bronze counts across all cup finishes), STREAKS (best daily, best weekly), HEAD TO HEAD (per-rival wins, zero-win rivals omitted, sorted desc). Uppercase display-font labels, right-aligned tabular-numeral values, no emoji; recomputed on panel open.
- QA hooks: `__race2.stats()` (driverStats), `__race2.paints()` (profile paint + unlock ledger + per-lock state), `__race2.unlockPaint(id?)` (force-earn one/all for QA).

**Tests**
- New permanent gate `test/stats.ts` — 64 checks, exit 1 on failure: roster shape (10/6/4, free paints byte-identical to a pinned list, colors unique, lock ids ↔ PAINT_LOCK_INFO sync); fresh-profile lock closure + free-paint selectability; MIDNIGHT exactly at knockout win + idempotent sweep; ULTRAVIOLET at exactly 25 near misses (24 no, 25 yes; 0→99 one-shot sweep); SUNBURST at 7-day streak (6 no, 7 yes; gap resets streak but keeps bestStreak + the earned paint); GOLD LEAF at weekly P1 (P2 no, P1 yes); unlockPaint idempotence; corrupt unlocks/stats sanitizers (unknown ids, dupes, non-numeric, empty keys); driverStats completeness on a simulated career incl. zero-win omission + sort + trophy-by-metal + both best-streaks; full reload round-trip (stats, unlocks, bestStreaks, selectability, quiet boot sweep after a finish-path sweep).
- Found+fixed during test authoring: none product-side — the two initial failures were test bugs (weekly bestStreak is 2 after two consecutive weeks; the reload section bypassed the app's finish-path sweep, so SUNBURST legitimately earned itself).

**Gates (all green)**: typecheck ✓ build ✓ laps 14/14 rivals 20/20 career 119/119 share 34/34 knockout 50/50 daily 59/59 probe 24/24 allocs PASS camera 28/28 ghostbattle 39/39 traffic 16/16 weekly 116/116 **stats 64/64**.

**Evidence** (qa/v8-phase5/, muted `?mute=1`, fresh profiles via localStorage seeding, not committed): garage-fresh-locked.png (4 dimmed swatches with requirement text: 25 CAREER NEAR MISSES / REACH A 7-DAY DAILY STREAK / WIN ANY KNOCKOUT RACE / WIN A WEEKLY GOLD TROPHY; 6 free at full saturation, Cyan selected); garage-locked-click-toast.png (Midnight click → LOCKED — WIN ANY KNOCKOUT RACE toast + shake, paint unchanged); boot-sweep-unlock-toast.png (stats seeded with 25 near misses pre-load → NEW PAINT UNLOCKED — ULTRAVIOLET toast on title load; STATS button visible in nav); garage-force-unlock-selected.png (`unlockPaint()` → 0 locked cells / 0 req labels / all selectable, Gold Leaf selected, profile.paint 0xd78a4a persisted); stats-page-populated.png (CAREER/RACING/TROPHIES/STREAKS all matching the seeded demo save) + stats-page-h2h.png (HEAD TO HEAD: SABLE 6 WINS, APEX 4 WINS, ONYX 1 WIN — correct singular); reload-persistence-garage.png (+ telemetry: all 4 unlocks, stats and trophies intact after page reload); race-smoke-midrace.png (sunrise-sprint autopilot at 136 km/h with ghost battle HUD — gameplay untouched).

**Verdicts (lead-reviewed, honest)** — Garage locks: clearly readable, requirement text legible at 8px caps and the dim/saturate treatment reads as "not yours yet" instantly; nit: row 1 grows taller than row 2 because Ultraviolet's label sits under it, so the two rows are visibly unequal — acceptable, and labels vanish once earned. Stats page: matches the existing achv-panel design language, tabular right-aligned values line up cleanly, head-to-head rows use the same row style so they don't feel bolted on; the panel scrolls (15 rows + h2h exceed one viewport at 800px) which is consistent with achievements. Unlock moment: boot-sweep toast lands over the title and reads perfectly; in-race toasts would compete with the achievement toast slot (single toast element) if both fire in one finish — rare, last-wins, same as today's achievement stack. Honest nits: (1) the garage 3D preview car keeps its built-at paint until a body change rebuilds it — pre-existing v2.2 behavior of `GarageSystem.renderPreview` (paint changes only live-update the race-scene car), left untouched; (2) GOLD LEAF keeps Copper's 0xd78a4a on purpose — recoloring would orphan profiles that had Copper selected; (3) near-miss lifetime stat counts raw passes, not the 5-per-race credited cap (career tally semantics, matches the requirement text "25 career near misses").

**Deviations / notes**
1. Locked paint names: spec wrote MIDNIGHT/SUNBURST/ULTRAVIOLET/GOLD LEAF; the roster already had Midnight (kept, case-insensitive match) and the other three are renames of Ultra/Magma/Copper with colors preserved — the byte-identical constraint applies to the 6 free paints only, and color-keyed saved profiles stay valid.
2. Head-to-head increments only on non-knockout rival finishes (career + free play) and knockout finishes where the player wasn't knocked out; daily/weekly stay lifetime-clean per their established guards (they DO still drive the streak/trophy unlocks through their own ledgers).
3. `knockoutWins` lifetime field added beyond the two spec'd fields — required so the boot sweep can evaluate MIDNIGHT from migrated/persisted state instead of only at the win event; doubles as a stats-page row (KNOCKOUT WINS).
4. `bestStreak` on DailySave/WeeklySave also added beyond the spec'd fields — "best daily/weekly streak" needs a high-water mark; both live inside their existing save objects and are monotonic by construction.

## Phase 6 — Interactive tutorial (2026-09-15, commit `v8 Phase 6`)

**Shipped**
- `src/game/tutorial.ts` (new): pure state machine (no three.js/DOM) + layout data. `TutorialRun(trackLength, emit)` advances 4 sequential drills on sunrise-sprint — STEER (3 gate rings), BOOST (2 orange pads), DRIFT (entry/exit sector rings, ≥1.2s cumulative drift between), BRAKE (final gate under 90 km/h). Events: `drill-start` (banner+hint), `hit` (targetId/done/total), `fail` (missed | too-fast | no-drift), `drill-clear`, `drill-skipped`, `complete` (skippedAny). Per-drill retry: fail resets the drill's targets and emits `fail`; 3rd fail emits `drill-skipped` and advances (max 3 tries per spec). `skipAll()` = completion-with-skips (SKIP button/ESC). Progress unwrap is wrap-safe both directions (forward lap wrap AND driving backward across the line can't spuriously trigger gates — the backward branch was added after live QA caught a +628m spurious jump).
- Drill layout (pinned by test vs real sunrise-sprint geometry: len 633.5m, checkpoints 210/420): steer rings dist 60 (+1), 140 (−1), 220 (+1); pads 365 (−1.5), 400 (+1.5); drift sector rings 450 → 545 (the sharpest stretch, peak ~10.2°/12m vs ≤6.4° at the steer gates — sector entry resets the drift clock and does NOT count toward the 1/1 gate, only the exit decides); brake gate 600 (corner-approach check, 33m before the line). Thresholds: gate miss >6m lateral, pad hit ≤2.2m, drift ≥1.2s, brake <90 km/h. All targets ≥8m from checkpoints, all fit inside the road with margin. `DRILL_START_DIST` (8/235/425/565) = where main.ts re-places the car after a failed attempt.
- Drift credit semantics: banks while the drift INPUT is held (grounded, speed >10 m/s) — `DRIFT_ACTIVE_MIN_SPEED`. Deliberate choice, documented in the module: the drill teaches "hold space", and the emergent `driftAmount` slip meter stays near zero on sunrise's gentle bend even when drifting (heading-vs-velocity slip only builds on sharp bends), so a slip-based bar would be unpassable at this track. `DRIFT_ACTIVE_AMOUNT` was removed in favor of the input-based rule.
- Visuals (builder.ts): `makeChevronTexture` exported; ring construction extracted into shared `makeGateRing()` used by BOTH the def-based track rings and the new `buildTutorialRig()` (same TorusGeometry 0.22 tube / additive 0x9df3ff×1.8 material family; pads reuse the chevron plane + additive material with an `#ff9a3d` orange canvas). No new mesh families. Rig is added to trackGroup on tutorial start and disposed in `teardownTutorial()` (geometries + materials + textures).
- main.ts wiring: `startTutorial()` (clears career/daily/weekly/rivals/knockout/traffic/drift modes → `startTrack(sunrise-sprint)` → race.practice=true (timer stopped, no checkpoints, no finish), writesRecords=false, maxGhosts=0, ghost visuals hidden → builds rig → HUD tutorial chrome on). Frame feed is allocation-free (preallocated sample object; `drifting` = input.drift && grounded && speed>10). `teardownTutorial()` runs at the top of `startTrack` and in `quitToMenu`, so every normal race path (RETRY/NEXT TRACK/career/daily/weekly/quit) is byte-identical to pre-P6 (practice=false, records on, zero tutorial state/visuals). ESC during tutorial = skip (intercepted before the pause chain; P still pauses). Fail → car re-placed at `DRILL_START_DIST[drill]` via `placeAt` (safe in practice mode; race self-syncs prevDist).
- HUD (hud.ts): persistent `.hud-tut-banner` (title + hint + progress line — GATES n/3 / PADS n/2 / DRIFT x.x/1.2s / UNDER 90 KM/H, quantized to avoid per-frame string churn), `.hud-tut-skip` button top-center (pointer-events on; click → skip), `showTutorial(on)` hide/show, `onSkipTutorial` callback. Splashes reuse `showSplash` with new classes: `splash-tut` (cyan — drill banner at first drill + NOW SET A TIME!), `splash-good` (green — DRILL CLEAR), `splash-bad` (red — TRY AGAIN / SKIPPED). First drill's splash is deferred to 950ms after GO so it never collides with the countdown flash.
- Completion: `finishTutorial(skippedAny)` persists `settings.tutorialDone: true` + `settings.tutorialSkipped` (additive optional flags, default-merge), fires the P9 `FRESH GRAD` placeholder (`window` CustomEvent `race2:tutorial-complete` {completed:!skippedAny} — achievement row itself is P9 scope), shows NOW SET A TIME!, holds the inert (status=complete) visuals for 1.7s under the banner, then drops into a REAL time-trial via `startTrack(same track)` (teardown runs inside startTrack before the rebuild — verified no lingering visuals live).
- Trigger: menus.ts PLAY → `if (!settings.tutorialDone && settings.onboarded) onStartTutorial() else show('tracks')` — fresh profiles (post-onboarding) get the tutorial exactly once; returning players never see it (legacy v2.2 profiles get onboarded=true force-set by save.ts, so they're "returning" by definition). SETTINGS gains a TUTORIAL row with REPLAY TUTORIAL (re-runs anytime).
- Save (additive): `Settings.tutorialDone?`, `Settings.tutorialSkipped?` — same optional-flag treatment as the hint* flags, no sanitizer needed, no schema bump.
- QA hooks: `__race2.startTutorial()` (force-start, returns drill summary) + `__race2.tutorial()` (mode/status/drill/tries/done/total/driftTime/skippedAny/ring world positions/pad ids/settings flags).

**Tests**
- New permanent gate `test/tutorial.ts` — 73 checks, exit 1 on failure: layout pins (ring/pad dists, ids, drill order, banner strings verbatim vs spec, thresholds) + geometry assertions against the real TrackCurve (all targets in-lap with margin, ≥8m from checkpoints, rings/pads inside road width, steer gates on ≤6.4°/12m ground, drift sector the sharpest stretch >0.15 rad/12m, brake gate gentler than the apex, restart points bracket each drill); state machine success paths per drill (steer 1/3→3/3→clear→boost, pad 1/2→2/2→clear→drift, entry-arm/exit-decide drift accounting incl. no banking outside the sector and entry not counting, brake speed gate, clean 4-drill complete); failure paths (off-line gate miss → fail tries 1/2 → third miss auto-skips → next drill + skippedAny latch; pad overshoot misses ×3 → skip; sector exit with <1.2s → no-drift fail → retry clears; 95 km/h gate cross → too-fast fail → under-90 retry clears); SKIP idempotence + observe-after-complete no-op; settings flags round-trip through simulated reloads; FULL scripted real-car run (CarPhysics + RaceController practice=true + autopilot line + input-based drift/brake coaching + main-mirroring fail-respawn) completes all 4 drills headless with zero fails/skips, timer stays 0.000, phase never finishes, PB/ghost/history all null, sim finite; shared TRACKS defs untouched.

**Gates (all green)**: typecheck ✓ build ✓ laps 16/16 (exit 0, 0 FAIL) rivals 20/20 career 119/119 share 34/34 knockout 50/50 daily 59/59 probe 24/24 allocs PASS camera 28/28 ghostbattle 39/39 traffic 16/16 weekly 116/116 stats 64/64 **tutorial 73/73**.

**Evidence** (qa/v8-phase6/, muted `?mute=1`, real gameplay via game autopilot + a setVirtual overlay injecting drift/brake coaching, not committed): 01-fresh-onboarding (WELCOME card on cleared localStorage); 02-drill1-steer-banner (STEER — PASS THE GATES + hint + GATES 0/3 + SKIP TUTORIAL button + two glowing rings visible down the straight + timer hidden practice-style); 03-drill1-rings-progress (GATES 1/3 after ring 1); 05-drill2-boost-pads (BOOST — RIDE THE ORANGE PADS + PADS 0/2 at 141 km/h); 06-drill3-drift-sector (DRIFT — HOLD SPACE THROUGH THE BEND + BANK 1.2s hint + live DRIFT 0.1/1.2s + car flying through the entry gate ring); 07a-drill4-brake (green DRILL CLEAR flash + BRAKE — CROSS UNDER 90 KM/H + final gate ahead at 72 km/h); 07b-now-set-a-time (NOW SET A TIME! splash at the line); 08-post-tutorial-time-trial-finish (the dropped-in TT finished by the autopilot: SUNRISE SPRINT panel, PB 0:19.275 written + ghost recorded — records work exactly as a normal TT); 09-settings-replay-tutorial (TUTORIAL row + REPLAY TUTORIAL button); 10a/10b SKIP path (SKIP TUTORIAL button; click → NOW SET A TIME + settings {done:true, skipped:true} verified in localStorage); 11-returning-play-tracks (returning profile PLAY → SELECT TRACK directly, PB 0:19.208 visible, no tutorial); 13-post-tutorial-clean-road (mid-TT road completely bare — no rings/pads/banner/skip, real timer + ghost battle HUD restored). ESC-skip verified via keypress (active→false, skipped:true persisted). Full-flow telemetry: all 4 drills complete in ~25s of driving with 0 fails (drift banked 1.93s; brake gate crossed at 68-72 km/h).

**Verdicts (lead-reviewed, honest; no subagent tool in this environment)** — Instructions readable: YES — banner title + hint + quantized progress line are legible at speed in all four drills, and the persistent banner means the instruction never disappears mid-drill (the 2.4s splash alone would have). Rings visible against v7 terrain: YES — the additive glow reads clearly against alpine meadow in daylight; the 06 shot shows the car inside the entry ring with the ring unmistakable. Pacing: YES — ~25s end-to-end for a clean run, each drill is a single pass, and the retry loop (respawn ~20-25m before the drill) keeps failures cheap. Honest nits: (1) the fail/TRY-AGAIN splash was exercised headless and via the same splash plumbing verified live (DRILL CLEAR/NOW SET A TIME shots) but I never captured the red TRY AGAIN frame itself — my scripted fail runs kept wedging the car into walls (open-loop steering through a 30ms write-war), a QA-tooling artifact, not a game bug; (2) the banner wraps to two lines for the two longest titles at 1280px — readable, slightly tall; (3) ESC-during-tutorial replaces pause (P still pauses) — per spec, but worth an AGENTS.md controls note at P10.

**Deviations / notes**
1. Drift drill credits the drift input (space held, grounded, >10 m/s) rather than the `driftAmount` slip meter — the slip meter stays ~0 on sunrise's gentle sector even while drifting (car.ts rebuilds velocity along the heading each step; slip only accumulates from align-to-frame on sharp bends), so a 0.3-slip bar would have been effectively unpassable at this track. Banner text still says HOLD SPACE; hint says BANK 1.2s.
2. Drift sector ENTRY ring arms the clock but does not count toward the drill's 1/1 gate (registerHit counts=false) — only the exit ring with ≥1.2s banked clears the drill; the entry still flashes green.
3. Pad misses (crossing a pad's dist >2.2m off-center) fail the boost attempt like a missed ring — the spec's fail list names rings/gates only, but without this a missed pad could leave the drill uncompletable on that pass.
4. Progress unwrap handles backward wrap too (driving backward across the line decrementing lapBase) — found live when QA scripting drove the car the wrong way through the start gantry and every gate misfired at once.
5. `laps` gate prints 16 PASS lines (14 track laps + steering-direction + placement checks), not 14 — pre-existing formatting; exit 0 with 0 FAILs is the pass condition.
6. First-drill splash is deferred to +950ms after GO (countdown GO! flash occupies the center slot before that); the persistent banner is present from drill-start regardless.

## Phase 7 — Audio 2.0 (2026-09-15, commit `v8 Phase 7`)

**Shipped**
- Per-body engine timbre (src/core/audio.ts): `ENGINE_TIMBRE` map keyed by player body — standard = neutral byte-compatible v2.2 voice (saw/square, cutoffMul 1, freqMul 1, sub 0.5); aero = brighter (cutoff ×1.3, osc2→sawtooth, freqMul 1.03 slight detune up, sub 0.42); tank = deeper (cutoff ×0.75, both oscs→square burble, freqMul 0.92, sub 0.70). `startEngine(body)` voices at spawn; `setEngineBody(body)` revoices the live engine (osc types + sub gain + filter base) on garage body change. updateEngine multiplies gear/rpm frequency and filter-cutoff targets by the voice multipliers. Rivals keep the neutral voice (no per-rival voices, per spec).
- Tire slip screech: continuous layer (shared loop noise buffer → bandpass 1.8–3.2 kHz → gain) created/stopped with the engine. Slip signal = `car.state.driftAmount` = `min(1, |lateral velocity| / 9)` (car.ts:260) — the existing continuous lateral-slip magnitude, gated `grounded && speed > 4` and zeroed at finish; commanded level exposed as `probe.slip.level`. Envelope `setTargetAtTime` attack tc 80 ms / release tc 200 ms, max 0.06 (cap 0.07 − margin). Deliberately separate from the existing drift-*entry* one-shot (`audio.drift()` on the 0.35 edge) — both textures coexist per spec.
- One-shots: `nearMissWhoosh()` (0.25 s band-pass noise sweep 350→2600 Hz, vol 0.14, subtle) wired to `traffic.onNearMiss`; `crash()` revoiced as low sine thump (64→30 Hz, 0.26 s, vol 0.5) + the existing noise crunch now tagged — the P3 `audio.crash()` hooks (wall, traffic contact, knockout, mover) all get the thud for free; `dailyFanfare()` (D-major rising fourths 587/880/1175, 0.5 s) fires on daily finish; `weeklyFanfare()` (fifth-based C arpeggio 523/784/1046/1319, 0.79 s, final held) fires on weekly final — distinct intervals/contours, both ≤1.5 s. Unlock chime kept as-is (polish not needed).
- Safety: master limiter (DynamicsCompressor threshold −12 / knee 6 / ratio 8 / 4 ms / 180 ms) between master and destination; `AUDIO_CEILINGS` exported (master 1, sfx 1, music 0.34, engine 0.16, wind 0.1, slip 0.07, musicLayer 0.5, oneShot 0.55) and asserted in the probe. Existing engine/wind/music layer gains already sit under the caps (verified at worst-case drive inputs).
- Probe introspection extended (additive): `engine` (osc types, sub, cutoffMul/freqMul, converged filterHz, live gain, voice), `slip` (gain/level/filter Hz), `gains` (all continuous bus/layer gains), `ceilings`, `limiter` — old fields byte-identical.

**Tests**
- `test/probe.ts` extended additively 24 → 61 checks, exit 1 on failure: engine/slip null-safe before start; limiter present; per-voice params pinned (osc types, sub, cutoff multiplier) + converged filter ratios aero/std = 1.30 and tank/std = 0.75 at identical drive inputs; slip envelope silent→0.0599 attack / release→0.0011; whoosh fires+decays (peak 0.140); crash emits thud+crunch pair (thud peak 0.5) and decays; daily/weekly fanfares tap-count 3/4, spans 0.50/0.79 s ≤ 1.5 s, distinct contours, levels ≤ one-shot ceiling, decayed; ceiling section walks every bus/layer at worst-case drive (master 0.9, sfx 1, music 0.34, engine 0.020, wind 0.081, music layers 0.35/0.21). All 24 legacy checks unchanged and passing. Mock gained `createDynamicsCompressor` (additive).
- Probe-fixture lesson: taps whose `setValueAtTime` is in the future evaluate at the mock's initial gain (1.0) — level assertions advance the virtual clock past all onsets first (documented in probe comments).

**Gates (all green)**: typecheck ✓ build ✓ laps 14/14 (exit 0) rivals 20/20 career 119/119 share 34/34 knockout 50/50 daily 59/59 **probe 61/61** allocs PASS (+0.000 MB/30 s) camera 28/28 ghostbattle 39/39 traffic 16/16 weekly 116/116 stats 64/64 tutorial 73/73.

**Evidence** (qa/v8-phase7/, muted, probe-driven, not committed): evidence.md (per-body timbre table, envelope table old+new buses, ceiling table, smoke notes, slip-signal documentation), probe-output.txt (61 PASS lines), smoke-race.png (mid-race muted, car + HUD live, 60 fps / ~103 draw calls). Muted browser smoke: rival + traffic + time-trial races via autopilot with zero console errors; live traffic race produced `nearMisses 2 → whoosh taps 2`, `collisions 2 → thud taps 2` with tap trail `go,go,thud,crash,whoosh,thud,crash,whoosh` mirroring gameplay exactly.

**Verdicts (lead-reviewed, honest)** — Supportive-not-flashy bar: met; the loudest new element (thud 0.5) sits below the existing boost burst (0.5) and lands only on real contacts, the whoosh (0.14) is quieter than the overtake chime it accompanies, and the screech tops out at 0.06 — felt, not noticed. Timbre differences are real synth deltas (types, cutoff, sub) not EQ presets, so aero/tank sound like different engines across the whole rpm range. Fanfares read as reward without stepping on the existing finish-medal jingle. Honest nits: (1) headless Chrome keeps the AudioContext suspended without a trusted gesture, so continuous-param motion is verified by the virtual-clock probe rather than live params — that is the phase's stated verification contract; (2) the screech shares the wind layer's 2 s noise loop (different filter band) — no audible correlation at these levels.

**Deviations / notes**
1. Slip signal uses `driftAmount` (|lateral velocity|/9) rather than a new raw lateral-velocity read — it IS the lateral velocity signal, already continuous 0..1, and gated consistently with the skid-mark/smoke thresholds; no car.ts change (physics conventions untouched).
2. Fanfares live in audio.ts (not music.ts) — all one-shot stingers + the tap/probe system live there; the roadmap's "may live in music.ts" was optional and music.ts remains pure pattern data.
3. `crash()` thud is a revoice, not a new hook — every P3 crash() call site gains the thump with zero call-site changes.
4. Engine restart on body change: the garage path rebuilds the scene (existing behavior), and `applyPlayerStyle` now revoices via `setEngineBody` before/without depending on that rebuild, so mid-menu body swaps are heard on the next race start even if the engine never stops.
5. Probe grew 24→61 checks; the roadmap's "probe 24/24" gate line is superseded by "probe 61/61" for P8+ (all legacy checks still individually present and passing).


## Phase 8 — Replay theater (2026-09-15, commit `v8 Phase 8`)

**Shipped**
- Session replay buffer (src/ui/replay.ts, new): `ReplayBuffer` keeps the last 5 time-trial replays in-session (`{trackId, samples, timeMs, dateMs}`, FIFO eviction). Entries are pushed in the finish handler under the exact WATCH REPLAY availability rule (`!rivalMode && !trafficMode`, recording ≥ 10 samples), so every watchable finish is stored automatically; the recording array reference is stable per race (controller allocates a fresh array per start), so no deep copy is needed.
- Theater state machine (main.ts): `startReplay()` scopes the buffer to the current track's entries (cross-track entries would render against the wrong scene geometry) and opens the latest; PREV/NEXT cycle that scoped list with wraparound. Entry state = `{samples, t, startMs, endMs (= last.t + 800, existing v2.2 tail), camPos, nextCut, speed, playing, autoCuts}`.
- Deterministic lookup: `sampleReplayAt()` (with `replayLookupAt()` binary search) is THE single interpolation path for playback, scrub-seeks, keyboard seeks, the photo-mode anchor and the manual-camera rig feed — pos lerp + quat slerp identical to the ghost-sample math, allocation-free, clamped at both ends. Playback advances `t` via `advanceReplayTime(t, dt·1000, speed)` clamped by `clampReplayTime`; scrubbing suspends the advance (playhead owned by the pointer) and resumes on release. Seeks clamp into `[startMs, endMs]` and, when auto-cuts is on, force a fresh director cut at the new playhead.
- Speed scale: 0.25×/0.5×/1× buttons (and 1/2/3 keys) scale ONLY the replay playhead — the fixed-step sim is untouched (the sim isn't even stepped during replay). Wheel spin scales with `dt × speed` so slow-mo reads slow; the camera rig's fake CarState speed is sample-space m/s, so the speed-FOV response still breathes with the action.
- Theater UI (TheaterUI, bottom overlay in the existing design language): full-panel-width scrub bar (pointer-captured drag = seek, fill + glowing dot, `m:ss.t / m:ss.t` time label), 0.25×/0.5×/1× speed group, CAM button (CHASE→CLOSE→HOOD), AUTO CUTS ON/OFF toggle, PREV/NEXT (buffer indicator `n/m` in the title), PAUSE/PLAY, EXIT, keyboard hint line. Keys while theater is shown: SPACE play/pause, ←/→ ±2 s seek, 1/2/3 speed, C camera, ESC exit — handled by the theater's own window keydown (repeat-guarded, shown-gated), never touching InputManager state; race keys are inert during replay anyway (sim not stepped) and edge-detected actions can't leak across state changes (verified live).
- Director (existing fixed-cut trackside v2.2 behavior) stays the default entry experience (cuts + flash every 6500 ms of replay time; bar briefly fades during cuts). Manual camera cycling turns the director OFF for that session (AUTO CUTS toggle re-enables); auto-cuts default = `!settings.reducedMotion` (reduced-motion users get a locked chase-style manual cam until they opt in). Manual modes use the P1 rig (`rig.update` on a minimal fake CarState fed from the shared lookup; hood POV hides the replay body exactly like the P1 race hood cam; rig mode is restored to `settings.cam` on exit and never persisted from the theater).
- Photo mode interplay: P still enters photo from the theater (replay time freezes = "P pauses" preserved); photo remembers where it came from (`photoFrom`), orbits the REPLAY car (not the parked player car) when opened from a replay, hides the theater bar, and restores theater + playhead state on exit. Pre-existing v2.2 bug fixed on the way: `exitPhoto` unconditionally returned to 'racing', which dead-ended a photo session opened from a replay.
- Two pre-existing director bugs fixed (they made scrubbing useless): (1) the cut camera hint used the PARKED PLAYER car's track index — once the hint window missed the replay car, the "trackside" cuts latched near the finish line (measured 27–180 m from the playhead); the hint is now the playhead's own frame (`replayCutHint`, reset per open), cuts sit 15–21 m from the car again; (2) a cut triggered by a big seek snap-cut instead of lerping across the map (>60 m jump snaps).
- Reduced-motion: `defaultAutoCuts(reducedMotion)`; shake/punch stay untouched. Touch layouts: the theater bar respects `--safe-*` insets; touch controls are already hidden during replay (existing `touch.hide()`), so no steer-zone occlusion; the hint line hides under 760 px.
- QA hook: `__race2.replay(cmd?, arg?)` — `open|seek|speed|cam|cuts|prev|next|pause|play` plus a full state readout (buffer contents, scoped count/index, t/total, playing, speed, cam, autoCuts, reducedMotion). `open` is guarded to state==='finished'.

**Tests**
- New permanent gate `test/replay.ts` — 34 checks, exit 1 on failure: ring-buffer FIFO eviction at 5 max with intact payloads; a REAL autopilot lap recording on sunrise-sprint (480 samples @30 Hz, monotonic); scrub determinism (1082-frame linear playback vs the same ts visited in seeded-shuffled order — pos < 1e-9, quat component-wise < 1e-12; plus 200 random ts sampled twice → bit-identical); speed math (60 frames @0.25× = +250 ms sample time exactly, 0.5× = +500 ms, full replay takes 4.00× wall frames, end/start clamps); edge lookups (before-start/after-end clamp to first/last sample, sample-space speed ~39.8 m/s mid-lap, single-sample refusal, bracket-search invariants); the theater manual-cam path over the full lap with modes cycling every 40 frames (0 NaNs, chase/close 100% in-frustum, hood 100% ahead-facing with fov ≥ 70); `defaultAutoCuts` and the REPLAY_SPEEDS table. Test-authoring note: `Quaternion.angleTo()` has ~1e-8 acos noise near dot=1 and cannot certify bit-identity — quat equality is compared component-wise.
- `test/allocs.ts` extended with a 30 s theater window (advance + shared lookup + fake-state rig with mode cycling, gc-forced) — +0.000 MB, PASS.

**Gates (all green)**: typecheck ✓ build ✓ laps 14/14 rivals 20/20 career 119/119 share 34/34 knockout 50/50 daily 59/59 probe 61/61 allocs PASS (incl. theater window) camera 28/28 ghostbattle 39/39 traffic 16/16 weekly 116/116 stats 64/64 tutorial 73/73 **replay 34/34**.

**Evidence** (qa/v8-phase8/, muted `?mute=1`, real finished laps driving the theater, not committed): 01-theater-open-director-cut (bar over the scene, car framed by a director cut, 0:02.1/4:02.0); 06-scrub-mid-drag (pointer captured mid-drag at 55% → t=2:13.1/4:02.0 = 133105 ms vs 133108 expected, cut flash visible on the seek); 07-quarter-speed-slowmo (0.25× lit, live probe: 2.5 s wall → 638 ms sample-time ≈ quarter rate); 08-hood-cam-replay (hood POV down the road, CAM HOOD + AUTO CUTS OFF); 11-manual-cam-autocuts-off (manual chase behind the replay car, cuts off); 09-buffer-entry2 / 10-buffer-prev-entry1 (2 buffered laps, NEXT/PREV cycles 2/2 ↔ 1/2 with per-entry totals 9:09.1 vs 0:18.3); keyboard probe (SPACE pause, ←/−2 s/→+2 s exact, 1/2/3 speeds, C cycles and disables cuts, ESC exits to the finish panel, no leak into post-exit input).

**Verdicts (lead-reviewed, honest)** — Scrub smooth: YES — pointer-captured drag re-derives the frame every move with zero drift (133105 vs 133108 ms is float display rounding), and the car is exactly where the bar says in every shot. UI readable over v7 scenes: YES — the dark panel + blur sits cleanly over both the alpine and neon scenes, the active speed/cut states read at a glance, and the cut-fade keeps flashes from fighting the bar. Honest nits: (1) the QA-session laps are wall-grindy (scripted P-driver), so the evidence replay is 4:02 long — human replays look better; (2) during director cuts the camera lerps to the new vantage for ~1.4 s, so mid-flight frames can be wide (existing v2.2 director feel, kept per spec); (3) the parked player car remains visible at the finish line during replays (existing v2.2 behavior, unchanged).

**Deviations / notes**
1. PREV/NEXT scope to the CURRENT track's buffer entries (spec says "cycles the buffer") — cycling into another track's replay would render it against the wrong scene; the 5-entry ring buffer itself stays global per spec, and same-track repeat laps (the realistic flow) get full cycling.
2. Photo-from-replay exit restores the theater instead of unconditionally returning to 'racing' (v2.2 would have dead-ended); racing photo flow unchanged.
3. Director hint fix + snap-on-far-seek + RAF-driven cut-fade clear are small behavior repairs to the v2.2 director made necessary by scrubbing (all covered above); no physics, save, or rival-mode changes anywhere in this phase.

## Phase 9 (completed by lead after subagent hit usage limit mid-phase)
- Subagent WIP finished: 5 new achievements wired (GHOSTBUSTER >2s friend-ghost busts, THREAD THE NEEDLE 5-nm traffic run, WEEKLY WARRIOR weekly gold, FRESH GRAD tutorial, PAINT COLLECTOR all locked paints) + save stats (friendGhostBusts, trafficNeedles) + panel rows.
- Traffic balance (subagent's tuning, lead-verified): pack 10-14 (was 8-12), speed band 0.50-0.65 (was 0.55-0.70), contact lat 1.5 (squeeze corridor [1.5,2.2)), contact cooldown 3s (episode not bump), near-miss bar 10 m/s, respawn gap 28m + train gap 24m. Harness: 14/14 finish, nm 3-8 on 11/14 (serpents-tail/neon-circuit below = short laps, fewer pass events — natural, not tuned to avoid overfitting; gauntlet-ii/twilight col 7-8 = non-dodging bot on twisty tracks, humans weave).
- Lead fix: SLICK MAYHEM on slick-less tracks was a no-op (only 3/14 tracks ship base slicks) — weeklyRaceTrack now synthesizes deterministic patches at 25/50/75% lap (alternating lateral, 4×24) when base has none; doubled zones unchanged on slick tracks.
- Traffic test pin 8-12 → 10-14 (additive, reflects balance).
- All 16 gates green post-changes (traffic 16/16, weekly 116/116, rest unchanged).
