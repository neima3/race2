# v9 progress

## Phase 1 (subagent hit rate limit mid-phase; lead finished)
- WIP completed: variant chips row (track select, settings.variant additive + sanitizer), free-play variant application (career/weekly/knockout keep own variants; ?variant= dev override keeps priority), dry-ledger policy via `variantWritesRecords()` → `race.writesRecords=false` + `recordReplay=true` so theater still works on variant laps; drift/traffic ledgers guarded; "VARIANT RACE — NO PB" finish note; `__race2.variant()` get/set hook.
- New permanent gate `test/variants.ts`: 4 variant laps complete headless (rain 17.78s vs 17.47 dry — grip flows), night race leaves PB null through the REAL finish path (structural writesRecords guard), day writes PB.
- All 17 gates green. Browser (muted): night neon-vertical + rain sunrise races, finish note, PB null check, chip row (`qa/v9-phase1/`).

## Phase 2
- Mechanic: `src/game/draft.ts` — pure `DraftTracker` (charge 0-1) + wrap-safe `gapAhead`. Pocket = 4-9m behind + |Δlat|<2.2 + **CLOSING** (gap shrinking; matched-following doesn't build — slipstream is a catch/slingshot reward, this qualifier is what keeps the rubber-band gap asserts and career pins intact). Build 0.5s, decay 0.3s, perk ≥0.5 charge → ×1.08 top speed via `CarPhysics.step(..., draftFactor = 1)` consumed in the existing speed-cap path (default 1 = byte-identical; the raised taper is the gentle accel assist — realized ≈ +4.4% equilibrium, measured headless 39.13→40.85 m/s).
- Wiring: player tracker in main.ts fixed-step (rival targets via `RivalManager.scanPlayerDraft` (alloc-free, shared with harnesses), traffic cars inline; ghosts never scanned). Collision resets: wallHit > 0.3 + traffic onContact. `race.draftFactor` field feeds the player step. HUD `setDraft` chip (cyan, cpWrap) + speed-line opacity floor while active; `__race2.draft()` + `state().draft` telemetry.
- **Rivals draft: disabled by default** (`RivalManager.draftEnabled = false`) — machinery kept + flag. Headless verification: with rivals drafting, the rival train outruns the rubber-band dead zone → rivals gate P4 (player failed to beat easy+mid) and knockout boundary eliminations collapsed (player KO'd P4 at boundary 1 on sunrise-sprint). Per spec fallback: player-only drafting, documented.
- **Draft scope: free rival + knockout + traffic only** — career/daily/weekly excluded (`draftMode` gate in main.ts): the career street-cup silver pin flipped gold with player drafting (autopilot collects the perk too; 1,1,1,3 → 1,1,1,2). Ledger modes keep pre-draft physics; P5 balance pass may revisit.
- New permanent gate `test/draft.ts` (15 checks): constants, build/decay/reset math, closing rule, pocket edges, gapAhead wrap, solo byte-identity (600 steps bitwise), +8%-cap realized top speed (scripted closing sawtooth), traffic drafting (kinematic lead, no collision), rivals race w/ production wiring (gap < 120m).
- Harness parity: test/rivals.ts + test/knockout.ts wire the player draft (production-style scan + `playerRef`); career/weekly left unwired (their modes don't draft).
- All 18 gates green (incl. new draft gate; laps byte-identical baselines). Browser (muted, `qa/v9-phase2/`): SLIPSTREAM chip lit behind rival pack (rival race, +7m pocket, 142 km/h), matched-follow decay (chip off while tucked — closing rule visible), post-slingshot P2 (chip off, FINAL LAP), traffic mid-pack NEAR MISS at 140 km/h; telemetry: draft-window top speed 41.5 vs solo max 40.1 m/s (+3.5%, confounded slightly by pads; headless +4.4% is the clean number). Verdicts: chip readable ✓; draft feel fair — brief windows, no auto-win (autopilot with draft still finished P2 behind APEX).

## Phase 3
- Codec: `buildReplayLink(def, timeMs, samples)` → `#r=v1.<trackId>.<timeMs>.<code>` — samples go through the SAME `encodeGhostCode` pipeline (quantize 15Hz payload + rle + deflate; no second quantizer). `parseReplayLink` symmetric via a shared `parseV1Envelope`; parsers reject each other's envelopes. `decodeGhostCode` now wraps deflate-stream failures as `ShareError('deflate failed')` (truncated codes previously leaked a raw TypeError; produced codes unchanged — `#g=` byte-identical). Oversize (>90s) trim + orientation/det sanity are inherent to the shared codec.
- Theater: SHARE REPLAY button (TheaterUI `onShare`/`setShareVisible`, hidden by default) — visible only for real recorded day laps: buffer entries now carry `variant` (push at finish) and `imported` flags; `theaterShareable()` = !imported && (undefined|day). Theater title gained a label slot (`FRIEND REPLAY` for imports). `shareReplay()` mirrors shareGhost: Web Share → clipboard fallback → `REPLAY LINK COPIED` toast, and dispatches `window` CustomEvent `race2:replay-shared` on success (P5 wires DIRECTOR).
- Import: `#r=` branch in checkShareHash (hash cleared via existing replaceState) → FRIEND REPLAY card (track + precise time + WATCH/DISMISS) → `watchFriendReplay` loads the track scene (if needed), builds a single-entry synthetic replayList `{imported: true}`, opens the theater directly (scrub/speeds/cams/auto-cuts all work; PREV/NEXT no-op within 1). **Persistence decision: session-only (in-memory `friendReplay` field) — no save change, documented.** Friend-replay EXIT returns to the title menu (no finish screen).
- UI layering fix: theater `.theater` z-index 18→40 (it is the active UI during replay; also unblocks automated clicks vs `.menu-layer` z-30), and `.menu-toast` moved out of `menu-layer` to `document.body` with z-index 60 so toasts read above the theater bar (found during QA: toast painted behind the bar).
- test/share.ts 34→51 checks: #r= envelope round-trip + parse symmetry (trackId/timeMs/code, cross-parse rejection), 30Hz lap through the codec (err 0.003m < 0.35m, det>0, time delta 0.002% < 1%), 60s replay code 6512 chars ≤ 12K, 90s trim, truncated/corrupt ShareError rejections.
- All gates green: typecheck, build, laps 14/14, rivals 20/20, career 119/119, share 51/51, knockout 50/50, daily 59/59, probe 61/61, allocs PASS, camera 28/28, ghostbattle 39/39, traffic 16/16, weekly 116/116, stats 64/64, tutorial 73/73, replay 34/34, variants, draft 15/15.
- Browser (muted, `qa/v9-phase3/`): autopilot TT 17.48s → theater → SHARE REPLAY → `REPLAY LINK COPIED` toast + 2365-char `#r=` URL + `race2:replay-shared` fired once (clipboard/share stubbed in headless: real `navigator.share` rejects InvalidStateError, `clipboard.writeText` hangs without permissions — production paths untouched); fresh profile → FRIEND REPLAY card (0:17.475) → WATCH → `FRIEND REPLAY · SUNRISE SPRINT · 1/1`, share button hidden → scrub (0:06.2) + hood cam ✓; replay end exits to title ✓; corrupt `#r=` → `INVALID REPLAY LINK` toast, hash cleared ✓. Verdict: full share loop works end-to-end.

## Phase 4
- Roster +4 (rivals.ts): NOVA mid 0xff3dd2 standard, KESTREL pro 0xff5c39 aero, VESUVIUS pro 0xffe14d tank, SOVEREIGN champion 0xffd23d standard (bodies visual-only for rivals, all paint values from existing palettes). `RivalTier` + `'champion'`; TIER_PARAMS.champion = pro skill (pace 1.0, ls 1.15, no noise/jitter), TIER_TUNING.champion = {accel ×1.07, maxSpeed ×1.04} → top 60.32 vs pro 59.45 (headless assert in test/rivals.ts).
- **Lineup determinism under roster growth**: growing the mid/pro pools silently reshuffles every seeded lineup (`floor(r*len)` shifts) — would have broken cups/daily/weekly/free-play byte-identity and the career balance pins. Fix: `V230_POOL_DEPTH = {easy:3, mid:3, pro:2, champion:0}` — pickLineup slots at/below the v2.3.0 depth draw from the frozen roster slice (in roster order); deeper slot keys + champion tier draw from the full pool. Proof: `test/tmp/lineup-baseline.ts` dumped all 16 cup slots + 14 free-play + 30 daily + 4 weekly lineups pre-change (git stash) — post-change diff = exactly one line, `grand-tour#3: APEX → SOVEREIGN`. Literals frozen into test/career.ts §3b (career 119→173 checks).
- Champion slot: `CupDef.championFinal` + `cupRaceTiers()` — grand tour R4 (serpents-tail, rain) swaps its last 'pro' for 'champion' → grid ROOKIE/SABLE/SOVEREIGN; easy+mid keys keep v2.3.0 seeds. Cups 1-3/daily/weekly/free-play lineups byte-identical. Cup points/standings absorb SOVEREIGN like any rival (finish panel shows SOVEREIGN P4 in cup classification).
- Banter: `tauntStart`/`tauntFinish` on all 12 presets (distinct, ≤48 chars; VESPER keeps the roadmap's "TRY TO KEEP UP."/"REMATCH. NOW."). GO splash: champion line when present, else roster-rotation; **driven by the frame loop (`banterPending`, performance.now + 950ms) not setTimeout** — headless CDP tab throttles timers to death (page setTimeouts never fire; synchronous DOM insertions work), and a timer-deferred splash was uncapturable/unshippable there. Finish splash: `RivalManager.onRivalFinish(name, paint)` → `tauntFor(name,'finish')` while `race.phase==='racing'` (rival/knockout/career/daily/weekly only — solo modes never run the manager). `.splash-banter` CSS: quieter than system splashes (smaller, near-white), static text, reduced-motion safe.
- KINGMAKER prep: at finish (proper finish only, not player-KO), if SOVEREIGN participated and the frozen standings place the player ahead → `championBeaten=true` + `window.dispatchEvent(new CustomEvent('race2:champion-beaten', {detail: {track, position, sovereignPosition}}))` (fired once per finish; knockout wins count, player KOs don't). New `__race2.champion()` → `{inRace, beaten}` (standings rows shape untouched).
- Gates: career 173/173, rivals 29/29 (+9: champion tuning vs pro, skill params, hasChampion/presets, banter completeness/uniqueness/format, legacy-tier freeze, newcomer order), share 51, knockout 50, daily 59, weekly 116, laps 14/14, camera 28, ghostbattle 39, traffic 16, stats 64, tutorial 73, replay 34, probe 61, variants, draft 15, allocs PASS, build + typecheck green.
- Browser (muted, `qa/v9-phase4/`): career hub grand-tour card unchanged (P2/P1/P2 + RACE 4/4 · 56 PTS + RESUME); GT R4 grid in rain — standings SOVEREIGN P1 gold dot, `champion()` = {inRace:true}; GO+1.1s splash "SOVEREIGN: THE THRONE STAYS MINE."; rival finish splash "SOVEREIGN: KNEEL. THEN TRY AGAIN." fired at 0:45.9 while the race continued; finish panel: SOVEREIGN P1 0:45.950 (+3.2s to SABLE, +5.2s to ROOKIE — champion visibly fastest), cup classification P4 SOVEREIGN 25pts; positive-path check (QA-injected paceBias 0.72 champion, sunrise-sprint, autopilot P1): `champion()` = {beaten:true}, event fired {position:1, sovereignPosition:4}. Verdicts: champion visibly faster ✓; banter readable, once per trigger, not spammy ✓. QA note: two event listener registrations in the QA session doubled the captured array (2 listeners × 1 dispatch) — harness artifact, not app behavior.
- FULL HOUSE achievement still thresholds at 8/`rivalsBeaten` — intentionally untouched (raising it would un-earn it for profiles that already cleared it; migration + KINGMAKER/DRAFT KING/STORM CHASER/DIRECTOR land in P5).

## Phase 5
- 4 new achievements (additive ids, existing pattern): `draft-king` DRAFT KING — finish a rival race (free rival or knockout win; career/daily/weekly never draft, traffic excluded) with ≥8s cumulative ACTIVE slipstream; `storm-chaser` STORM CHASER — **documented interpretation of the roadmap's "win a night rain variant race"**: complete a NIGHT variant solo race AND a RAIN variant solo race (variant picker drives solo modes only; traffic runs + dusk count for neither); `director` DIRECTOR — share a replay (`race2:replay-shared` listener in Game constructor: stat + pop announce — theater shares happen outside finish flows); `kingmaker` KINGMAKER — beat SOVEREIGN (`race2:champion-beaten` listener writes the stat only; the finish flow's achvBefore→after diff announces the pop, no double toast). Panel rows + counters in buildAchievements; stats additive (`draftKingRaces/nightRaces/rainRaces/replaysShared/championBeaten`) with sanitize + addStats plumbing; pops idempotent (stat-driven, no boot sweep needed — every new counter starts at 0 for every profile, nothing to migrate).
- Draft cumulative-time accumulator: `DraftTracker.draftTime` (public field) accrues only while `active`; `reset()` (wall hit/respawn/traffic contact) deliberately does NOT clear it — a new race zeroes it in startTrack. `__race2.draft()`/`state().draft` expose `time` (extend-only).
- Balance tables (headless, autopilot, full gates below):
  - **Draft** (2-lap rival races, production wiring, autopilot passive): sunrise-sprint 3.33s/34.1s race, dune-rush 4.17s/47.1s, harbor-nine 0.64s/47.8s, grand-gauntlet 3.77s/61.8s — nothing at 0, nothing >30s → no pocket-geometry tuning. DRAFT KING 8s sits above the passive-autopilot band (0.6-4.2s) deliberately: the closing qualifier caps matched-follow farming, so 8s rewards deliberate slingshot cycles (skill-based, not grindy). Browser confirmation: real rival race accumulated 3.2s mid-race via `__race2.draft().time`; `draftKingRaces` correctly stayed 0. Traffic pockets in real races are sub-300ms (closing 15m/s through the 5m window) — chip flickers, near-miss flow unaffected (3 near misses in the QA run).
  - **Variants**: night + rain laps complete headless on 3 tracks (sunrise-sprint existing + dune-rush 23.52s/24.14s + harbor-nine 24.23s/25.25s) — test/variants.ts extended additively.
  - **Champion** (`test/tmp/sovereign-balance.ts`, GT R4 serpents-tail, seeded cupLineup): dry control player P1/-19m; **RAIN (production GT R4 conditions) SOVEREIGN P1, player P2 at −9m** (matches P4 browser QA: champion usually wins); rain + player accel×1.04/top×1.02 → player P1/−17m; rain + champion tuning → player P1/−19m. SOVEREIGN: usually P1, beatable with a strong lap ✓.
- Full QA sweep (muted, headed window for unthrottled autopilot timers, `qa/v9-phase5/`, not committed): variant chips ×4 selected+persisting (settings.variant verified in storage); night race (stars, 144 km/h) + rain race (streaks) through the REAL finish path → nightRaces/rainRaces = 1/1 each, "VARIANT RACE — NO PB" note shown, PB stayed null → STORM CHASER popped live; slipstream chip captured LIT in a rival pack (organically triggered, held visible by an in-page MutationObserver QA patch for the screenshot — activation was real gameplay); replay share → `REPLAY LINK COPIED` toast + `race2:replay-shared` fired exactly once + replaysShared=1 + DIRECTOR popped live (clipboard/share stubbed in-page); 2371-char `#r=` URL → fresh profile (localStorage.clear) → FRIEND REPLAY card (0:17.475) → WATCH → `FRIEND REPLAY · SUNRISE SPRINT · 1/1`, SHARE button hidden, hash cleared ✓; champion grid ROOKIE/SABLE/SOVEREIGN + GO splash "SOVEREIGN: THE THRONE STAYS MINE." + `champion()` = {inRace:true}; grand-tour card RESUME · RACE 4/4 · 56 PTS unchanged; touch portrait 390×844 (◀ ▶ BRAKE GAS + compact HUD, nothing clipped); reduced-motion rival race completes, no finish slow-mo (`state().slowmo` false); old-profile migration: pre-v8 profile (no onboarded/tutorialDone/variant keys, legacy stats) boots clean — onboarding skipped, tutorial not forced, PLAY → track select, legacy-derived rows (FIRST BLOOD/SOCIAL CLIMBER) earned, v9 rows locked, new stats default 0; achievement done-state surgery → all 4 rows ✓ with counters, boot after = zero toasts (idempotent).
- Perf census (dev build, in-race): rain-rivals GT R4 — **fps 60 flat, 128 draw calls ≤ 220**; allocs gate PASS (headless).
- QA friction notes (harness, not app): headless CDP tabs throttle `setInterval` → `__race2.auto` ticks starve (documented P4; QA ran in a headed window); draft chips latch for <400ms so screenshots need the observer-hold trick; planted `careerRun` must include a `tracks` envelope or loadSaves rejects the whole blob (found via QA surgery — app behavior correct).
- Gates after changes: typecheck + build + laps 14/14 + rivals 29/29 + career 173/173 + share 51/51 + knockout 50/50 + daily 59/59 + probe 61/61 + allocs PASS + camera 28/28 + ghostbattle 39/39 + traffic 16/16 + weekly 116/116 + **stats 80/80** (extended: 15-row roster, id/name pins, flip semantics, pop diff, sanitize/round-trip) + tutorial 73/73 + replay 34/34 + **variants** (extended: 3-track night/rain) + **draft 21/21** (extended: draftTime accrual/reset semantics + DRAFT_KING_SECONDS + autopilot cumulative-time band 2-30s).
