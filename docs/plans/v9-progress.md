# v9 progress

## Phase 1 (subagent hit rate limit mid-phase; lead finished)
- WIP completed: variant chips row (track select, settings.variant additive + sanitizer), free-play variant application (career/weekly/knockout keep own variants; ?variant= dev override keeps priority), dry-ledger policy via `variantWritesRecords()` → `race.writesRecords=false` + `recordReplay=true` so theater still works on variant laps; drift/traffic ledgers guarded; "VARIANT RACE — NO PB" finish note; `__race2.variant()` get/set hook.
- New permanent gate `test/variants.ts`: 4 variant laps complete headless (rain 17.78s vs 17.47 dry — grip flows), night race leaves PB null through the REAL finish path (structural writesRecords guard), day writes PB.
- All 17 gates green. Browser (muted): night neon-vertical + rain sunrise races, finish note, PB null check, chip row (`qa/v9-phase1/`).

## Phase 2
- Mechanic: `src/game/draft.ts` — pure `DraftTracker` (charge 0-1) + wrap-safe `gapAhead`. Pocket = 4-9m behind + |Δlat|<2.2 + **CLOSING** (gap shrinking; matched-following doesn't build — slipstream is a catch/slingshot reward, this qualifier is what keeps the rubber-band gap asserts and career pins intact). Build 0.5s, decay 0.3s, perk ≥0.5 charge → ×1.08 top speed via `CarPhysics.step(..., draftFactor = 1)` consumed in the existing speed-cap path (default 1 = byte-identical; the raised taper is the gentle accel assist — realized ≈ +4.4% equilibrium, measured headless 39.13→40.85 m/s).
- Wiring: player tracker in main.ts fixed-step (rival targets via `RivalManager.scanPlayerDraft` (alloc-free, shared with harnesses), traffic cars inline; ghosts never scanned). Collision resets: wallHit > 0.3 + traffic onContact. `race.draftFactor` field feeds the player step. HUD `setDraft` chip (cyan, cpWrap) + speed-line opacity floor while active; `__race2.draft()` + `state().draft` telemetry.
- **Rivals draft: disabled by default** (`RivalManager.draftEnabled = false`) — machinery kept + flag. Headless verification: with rivals drafting, the rival train outruns the rubber-band dead zone → rivals gate P4 (player failed to beat easy+mid) and knockout boundary eliminations collapsed (player KO'd P4 at boundary 1 on sunrise-sprint). Per spec fallback: player-only drafting, documented.
- **Draft scope: free rival + knockout + traffic only** — career/daily/weekly excluded (`draftMode` gate in main.ts): the career street-cup silver pin flipped gold with player drafting (autopilot collects the perk too; 1,1,1,3 → 1,1,1,2). Ledger modes keep pre-draft physics; P5 balance pass may revisit.
- New permanent gate `test/draft.ts` (15 checks): constants, build/decay/reset math, closing rule, pocket edges, gapAhead wrap, solo byte-identity (600 steps bitwise), +8%-cap realized top speed (scripted closing sawtooth), traffic drafting (kinematic lead, no collision), rivals race w/ production wiring (gap < 120m).
- Harness parity: test/rivals.ts + test/knockout.ts wire the player draft (production-style scan + `playerRef`); career/weekly left unwired (their modes don't draft).
- All 18 gates green (incl. new draft gate; laps byte-identical baselines). Browser (muted, `qa/v9-phase2/`): SLIPSTREAM chip lit behind rival pack (rival race, +7m pocket, 142 km/h), matched-follow decay (chip off while tucked — closing rule visible), post-slingshot P2 (chip off, FINAL LAP), traffic mid-pack NEAR MISS at 140 km/h; telemetry: draft-window top speed 41.5 vs solo max 40.1 m/s (+3.5%, confounded slightly by pads; headless +4.4% is the clean number). Verdicts: chip readable ✓; draft feel fair — brief windows, no auto-win (autopilot with draft still finished P2 behind APEX).
