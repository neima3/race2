# v10 progress — "New Metal, New Miles"

## Phase 1 — GLIDE body
- **Tuning (src/physics/car.ts `BODY_TUNING.glide`):** `driftGrip 1.71 (−10%), accel 32.64 (−4%), grip 7.35 (−2%), maxSpeed neutral`. **Documented deviation from the roadmap's "driftGrip +10%": the roadmap's sign is mechanically inverted for its own stated intent.** In this engine `driftGrip` is the drift-mode lateral-decay rate (`vL *= 1 − driftGrip·dt`, car.ts): HIGHER = slides die FASTER. Measured (test/tmp/drift-balance.ts): slide-retention probe (40 m/s, +6 m/s lateral impulse, drift held): standard 0.42s / aero 0.40s / tank 0.42s / +10% candidate 0.38s (shorter than standard — fails "longest drifts") / shipped −10% 0.47s (longest ✓). Drift-mode lap score (drift held, pure-pursuit steer lag 27×120Hz steps ×0.9 + centering damper): serpents-tail standard 113 vs +10% candidate 8 vs **shipped glide 298** — only the −10% tuning outscores standard on the twisty track. accel/grip/top-speed deltas exactly per spec.
- **Drift identity verification** (headless, `test/tmp/drift-balance.ts`, production score formula `driftAmount·speed·12` gated >0.3): serpents-tail (twisty, banked): standard 113 / aero 230 / tank 164 / **glide 298** (drift time 1.8s vs standard 0.7s — outscores ✓). salt-flats (speed): standard 74 / tank 37 / **glide 50** (roughly matches ✓; aero 424 is the regime's wall-scrape outlier). Engine note (v8 P6 documented, re-confirmed): velocity is rebuilt along the heading each step, so slip only accumulates from align-to-frame on sharp/banked bends + walls — drift-mode laps need the lagged-steer drifter, pure pursuit never slips.
- **Visual (src/render/car-model.ts):** `CarBodyStyle` + 'glide'; long-tail coupe in `buildCarVisual` style switch — chassis 1.6w×0.32h (sleeker), long-tail deck box stretching the tail to z −2.45, main wing relocated onto the deck (z −2.12) on twin pylons, stacked lower wing element (the "extension", z −2.36), tail diffuser, per-style accent endplate geo (`accent:${style}` key — positions byte-identical ±0.85 for standard/aero/tank). Same 8-mesh draw-call class as existing bodies; traffic geometry stays 3-style per spec. ENGINE_TIMBRE.glide (audio.ts): sawtooth+triangle, cutoff ×1.1, freq ×0.97, sub 0.55 (probe untouched, 61/61).
- **Unlock:** `bodyUnlocks(totalStars, hasTrophy, championBeaten)` — glide = `championBeaten >= 1`, req `BEAT SOVEREIGN`. Live unlock moment wired into the existing `race2:champion-beaten` listener (main.ts): first-ever beat (stat 0→1) toasts `NEW BODY UNLOCKED — GLIDE` + chime; derived gate (no new save field, matches aero/tank pattern). Locked card click now toasts `LOCKED — <req>` (mirrors paint swatches; applies to aero/tank too). Selection persists via profile.body; sanitizer accepts 'glide', corrupt → standard.
- **Garage:** 4-card loop includes glide; `bodyStatRatios('glide').drift = 1.1` display value (driftGrip decay runs inverse to slide length — bar shows the measured identity) → DRIFT bar 77% = longest in the garage (aero 73%, standard/tank 70%). CSS: `.body-grid` wraps + `flex: 1 1 230px; min-width: 0` — 4 cards squeezed GLIDE's stat tracks to 7px (TANK's nowrap lock text ate the row); verified all tracks 193px after fix.
- **Harness:** `--body=glide` in test/laps.ts; 14/14 ×4 bodies (standard/aero/tank/glide), steering-direction + placement PASS per body (glide heading·right −0.999, lateral −5.64). Glide laps within +0.1..+0.2% of standard on most tracks (grand-gauntlet faster: 29.63 vs 30.83 — lower grip rotates cleaner); salt-flats 22.35 vs 22.18.
- **Tests:** test/stats.ts 80→94 (+14: tuning pins for all 4 bodies byte-identity, stat-bar ratios, unlock gate at exactly championBeaten=1, no aero/tank leak, idempotence, selection persistence, corrupt-body sanitize).
- **Gates:** typecheck ✅ build ✅ laps 14/14 (std) ✅ laps ×aero ✅ ×tank ✅ ×glide 14/14 ✅ rivals 29/29 ✅ career 173/173 ✅ share 51/51 ✅ knockout 50/50 ✅ daily 59/59 ✅ probe 61/61 ✅ allocs PASS ✅ camera 28/28 ✅ ghostbattle 39/39 ✅ traffic 16/16 ✅ weekly 116/116 ✅ stats 94/94 ✅ tutorial 73/73 ✅ replay 34/34 ✅ variants ✅ draft 21/21 ✅.
- **Browser QA (muted `?mute=1`, Playwright+system Chrome, `qa/v10-phase1/`, not committed):** fresh profile garage — GLIDE locked `BEAT SOVEREIGN` (01), locked click toast `LOCKED — BEAT SOVEREIGN` (02); `race2:champion-beaten` event with stat 0 → toast `NEW BODY UNLOCKED — GLIDE` + championBeaten persisted 1 (03); forced-unlock garage — GLIDE unlocked, selected, 2×2 cards, DRIFT bar visibly longest (04/05); GLIDE on serpents-tail grid — long-tail + stacked wing silhouette reads clearly (07); **in-race drift at 71 km/h — DRIFT +34 chip lit, drift smoke, skid marks, car visibly sideways (08)** via distance-keyed playback of the verified headless drifter's input schedule (recording: test/tmp/record-drift-inputs.ts; 624 1m-samples covering the 628m lap, driftScore 200).
- **Verdicts (honest):** silhouette distinct at a glance — YES (extended deck + dual-element wing; preview + in-race both read). Drift identity felt in telemetry — YES (retention 0.47s vs 0.42s; drift-lap score 298 vs 113; in-race chip + smoke + skids). Caveat: the in-race drift shot shows the car sliding at the road edge (scrappy line, offroad-adjacent) — real drift, honestly captured from a scripted drifter, not a hand-tuned beauty lap. Stat bars honest — YES post-CSS fix (widths 67-77%, drift maxes).
- **QA friction (harness, not app):** headless swiftshader runs the game at ~15fps and CDP `captureScreenshot` hangs on the RAF/WebGL loop under agent-browser — Playwright worked; time-keyed input playback diverges at low fps (fixed by distance-keyed replay); agent-browser's screenshot command never produced output on this page (kept for snapshot/eval only).

## Phase 2 (subagent died silently mid-phase; lead finished)
- summit-run authored by subagent had real defects (probe: 14 same-level overlaps near seam, seam approach −18m, 41 bot respawns). Lead reworked the descent: summit sweep around the east edge, diagonal merge into the seam → 0 hard/0 stacked/0 near overlaps.
- Probe's own respawn counter was buggy — standard harness = 0 respawns, clean 30.79s lap.
- Medals calibrated from real baseline: author 32.5s / gold 37 / silver 43.5 / bronze 55.5. Ghost emitted (additive +1 line). TRACKS append-only; star counter → 64 dynamic.
- Daily rotation fix (lead): the anti-repeat compared today's final index to yesterday's RAW hash — broke when n changed to 15. Replaced with a deterministic epoch walk (guaranteed no-consecutive-repeats by construction, memoized; hash cost ~1/call). daily/weekly rotation baselines regenerated; the 14 legacy free-play lineups verified byte-identical (frozen V230 slices) and pinned; summit-run = new-capacity lineup 'JUNO/ONYX/APEX'.
- Gates: laps 15/15 (summit PASS), career 173/173 (updated rotation pins), daily 59/59, weekly 116/116, rest green. Browser: summit-run race + minimap (`qa/v10-phase2/`).

## Phase 3 — halo-flats (mesa rings speed-lap, track #16)
- **Authoring (src/track/defs.ts TRACK_16, appended at END):** mesa, `variant: 'night'` (free-play rival/knockout default night via `resolveVariant`'s `def.variant ?? 'day'`; the P4 APEX cup will assign night explicitly too). Wide 11m road (10.5 on banked arcs), **pure stadium: two R≈130m banked 180° sweepers (banks 7-10°) joined by 70m straights** — all long fast sweepers, perfectly flat (y=0, GROUND_Y −0.45 clears; extent 276m). len 952m, kink 1.3°, **0 hard / 0 stacked / 0 near overlaps**, seam approach gap −22 (same class as salt-flats's parallel-straight wrap, hard-scan 0). Accent 0x7dffdf (mint), subtitle "Thread five halos flat out". 2 checkpoints (330/660), 2 boost pads ON STRAIGHTS (d40, d515, strength 10), no slicks/movers.
- **Rings (5, existing mechanics — placement only; hit = `main.ts` `pos.distanceTo(ring) < radius` → `applyBoost(7,1.2)`, browser-only so headless baseline is a no-ring lap):** #0 d38 lat 0 h1.6 (front-straight gate), #1 d180 lat 0 h2.0, #2 d274 **lat 2.8** (north apex — the pure-pursuit line swings OUTSIDE on the banked arc, lat +4.3 measured; lat 0 missed at 4.7m, moved onto the wide line), #3 d370 **lat 3.5** (late arc — same outside-line effect downstream of #2's boost), #4 d750 lat 0 h2.0 (south apex crown). All radius 3.8 (dune-rush/ring-runner family size). **Hit-check: 5/5 on the fully-boosted autopilot line** (probe: `test/tmp/halo-probe.ts`, summit-probe pattern + ring world-position/hit/boost replication; laps-harness loop as respawn truth).
- **Baselines:** headless autopilot no-ring **23.01s, 0 respawns** (laps-harness loop); with rings applied **19.79s (−3.2s)** — ring-line mastery worth ~3s. Medals from the actual no-ring baseline ×1.05/1.2/1.4/1.8 → 500ms rounds: **author 24.0 / gold 27.5 / silver 32.0 / bronze 41.5**. Ghost emitted additively (16 fresh + 0 carried). Per-body laps: aero 22.47 / tank 23.19 / glide 23.22 — all PASS 16/16.
- **Rotation impact (n 15→16):** daily 30-day + weekly 4-week baselines regenerated in test/career.ts from the implementation (walk/shuffle shift as designed; deterministic invariants unchanged). Free-play: 14 legacy pins + summit-run 'JUNO/ONYX/APEX' verified **byte-identical** (frozen slot keys 0-2); halo-flats = new-capacity entry 'JUNO/ONYX/APEX'.
- **Gates:** typecheck ✅ build ✅ **laps 16/16** ✅ rivals 29/29 ✅ career 173/173 (updated baselines) ✅ share 51/51 ✅ knockout 50/50 ✅ daily 59/59 ✅ probe 61/61 ✅ allocs PASS ✅ camera 28/28 ✅ ghostbattle 39/39 ✅ traffic 16/16 ✅ weekly 116/116 (updated) ✅ stats 94/94 ✅ tutorial 73/73 ✅ replay 34/34 ✅ variants ✅ draft 21/21 ✅.
- **Browser QA (muted `?mute=1`, playwright-core + system Chrome/swiftshader, `qa/v10-phase3/`):** ring-line shot 01 — car at 190 km/h on the banked north apex under glowing ring #2, second halo visible up the arc; night variant wide 02 (`?variant=night`) — headlight cone, three halos down the dark arc; minimap 03 — stadium outline + player dot on the back straight over the boost chevrons; PB **null before first finish → 19950ms after the day autopilot finish** (cross-validates the headless 19.79s ring-boosted lap; night run finished dry, no write — variants policy); finish panel 04 shows AUTHOR MEDAL at 19.949s.
- **Notes/deviations:** (1) PRE-EXISTING clearance failure on canyon-twist at HEAD (`road dips to −0.41 vs −0.40 threshold`, v7 geometry repairs residue) — verified via `git stash` that it predates P3; halo-flats itself passes clearance clean (minRoadY 0.00, extent 276m, landform/rock margins ok). (2) Author medal is reachable only with clean ring hits (browser autopilot 19.95 vs author 24.0 — generous headroom for humans; no-ring humans sit ~23s ≈ author band). (3) First layout (kidney, 15 CPs) hit 25.8s + 11.5° seam kink; replaced with the stadium — simpler, faster, cleaner seams.

## Phase 4 — APEX LEAGUE (5th cup) + achievements
- **Cup spec (src/game/career.ts):** `apex-league` — APEX LEAGUE, accent 0xffd23d (champion gold), subtitle "All-pro, dusk to rain, for the throne", grid ALL-PRO. **summit-run (dusk) → halo-flats (night) → volt-alley (day) → grand-gauntlet (rain)**, `championFinal: true` → R4 tiers resolve `pro/pro/champion` (SOVEREIGN defends the throne at grand-gauntlet; roster guard respected: 2 pro + 1 champion ≤ capacity). **Unlock (custom rule, new `CupDef.unlock {chainTrackIds, trophyOfCupId}` in `cupUnlock`):** grand-gauntlet AND volt-alley unlocked via the natural track chain (medal on sky-loop / ring-runner respectively) AND any GRAND TOUR trophy; locked card shows the escalating requirement (`UNLOCK GRAND GAUNTLET` → `UNLOCK VOLT ALLEY` → `GRAND TOUR TROPHY`); `?alltracks` bypasses. Tier mix races 1-3: all-pro (`['pro','pro','pro']`, third slot = deep draw).
- **Achievements (src/game/achievements.ts, additive ids):** `apex-champion` APEX CHAMPION (derived: any apex-league gold finish in the cup ledger — no new stat), `glide-rider` GLIDE RIDER (new additive stat `glideWins`: rival/knockout win while `profile.body === 'glide'`, wired into both finish branches in main.ts), `ringmaster` RINGMASTER (new additive stat `ringPerfectLaps`: per-lap ring counter `ringLapHits` reset at race start, incremented on ring hit, window closed by `closeRingLap()` on both the `lap` event AND the finish event — the final crossing emits finish, not lap; halo-flats-only, any mode). Panel rows in menus.ts (roster 15→18 rows), pops idempotent via the standard before/after diff. Save additive: `glideWins`/`ringPerfectLaps` in LifetimeStats + defaults + sanitize + addStats.
- **Lineup determinism:** `test/tmp/lineup-baseline.ts` regenerated + diffed against the pre-change capture — **exactly 4 added lines, 0 changed lines**: `apex-league#0 APEX/VESPER/VESUVIUS`, `#1 APEX/VESPER/VESUVIUS`, `#2 APEX/VESPER/KESTREL` (deep pro slot reaches the v9 roster), `#3 APEX/VESPER/SOVEREIGN` (champion swap). Existing 4 cups × 4 + 16 free-play + 30 daily + 4 weekly byte-identical. Apex lineups frozen in test/career.ts `FROZEN_CUPS` from birth.
- **Gates:** typecheck ✅ build ✅ laps 16/16 ✅ rivals 29/29 ✅ **career 213/213** (173→213: apex spec/unlock-chain/champion-R4/frozen-lineup pins + 10b achievement section) ✅ share 51/51 ✅ knockout 50/50 ✅ daily 59/59 ✅ probe 61/61 ✅ allocs PASS ✅ camera 28/28 ✅ ghostbattle 39/39 ✅ traffic 16/16 ✅ weekly 116/116 ✅ **stats 100/100** (94→100: v10 counters + roster 18) ✅ tutorial 73/73 ✅ replay 34/34 ✅ variants ✅ draft 21/21 ✅.
- **Browser QA (muted `?mute=1`, playwright-core + system Chrome/swiftshader, `qa/v10-phase4/`):** career hub renders **5 cards**, fresh-profile apex locked `LOCKED — UNLOCK GRAND GAUNTLET` (01/02); forced-unlock hub shows apex `RESUME · RACE 4/4` (06) → interstitial RACE 4/4 GRAND GAUNTLET with seeded standings (07) → grid is grand-gauntlet **rain** with SOVEREIGN P3 on the tower, `champion().inRace = true` (08); autopilot R4: **SOVEREIGN won the race on track (P1)** — player P4 +3.5s but banked points took the cup → GOLD TROPHY + `ACHIEVEMENT UNLOCKED — APEX CHAMPION` toast, run cleared, trophy persisted (09). RINGMASTER: real autopilot halo-flats lap through the halos at 188 km/h (03) → AUTHOR medal 19.933s finish with `ACHIEVEMENT UNLOCKED — RINGMASTER` toast, `ringPerfectLaps = 1` in localStorage (04). GLIDE RIDER: GLIDE-body rival race P1 → toast + `glideWins = 1` (05). All 3 v10 achievements flip `done` in `achievements()` live; achievement roster 18 rows on a fresh profile.
- **Verdicts (honest):** cup flow end-to-end through the REAL menu path (hub → interstitial → grid → finish → trophy) — YES. RINGMASTER evidence fully organic (autopilot's real 5/5 ring line, no assists). GLIDE RIDER P1 secured with the `finishLine()` QA teleport assist on both laps (autopilot alone finished P2 on merit vs the easy/mid/pro free-play grid; the win-path stat wiring itself is the code under test and fired correctly). APEX CHAMPION run used a seeded 3-win ledger to skip races 1-3 (documented; the real 4-race sweep is Phase 5's end-to-end QA). Champion defense reads correctly: SOVEREIGN beat the field on track yet the cup went on points.
- **QA friction:** the `finishLine()` hook is defeated by the checkpoint guard when triggered mid-lap on short tracks (len−250 is half of sunrise-sprint) — near-line trigger (len−70) works; noted for future QA scripts.

## Phase 5 — balance tables + full QA sweep
- **Gates (all green at HEAD, pre-fix baseline):** typecheck ✅ build ✅ **laps 16/16 ×4 bodies** (std/aero/tank/glide) ✅ rivals 29/29 ✅ career 213/213 ✅ share 51/51 ✅ knockout 50/50 ✅ daily 59/59 ✅ probe 61/61 ✅ allocs PASS ✅ camera 28/28 ✅ ghostbattle 39/39 ✅ traffic 16/16 ✅ weekly 116/116 ✅ stats 100/100 ✅ tutorial 73/73 ✅ replay 34/34 ✅ variants ✅ draft 21/21 ✅.
- **GLIDE balance table — pure harness pace (headless autopilot, no rings):** salt-flats std 22.18 / aero 21.62 / tank 22.38 / **glide 22.35** (+0.17 vs std); halo-flats std 23.01 / aero 22.47 / tank 23.19 / **glide 23.22** (+0.21 vs std, slowest of 4 — no speed-track domination); summit-run std 30.79 / glide 31.02.
- **GLIDE drift identity (`test/tmp/p5-balance.ts`, production score formula, drift-mode laps ×4 bodies):** serpents-tail anchor: std 113 / aero 230 / tank 164 / **glide 298** (driftT 1.8s — the twisty identity, unchanged from P1). salt-flats anchor: std 74 / tank 37 / glide 50 (aero 424 = wall-scrape outlier). **NEW-TRACK FINDING (recorded, not a bug): on summit-run NO body generates meaningful slip in drift mode (glide/aero/tank maxD ≈ 0.01, standard 0.55 from 2 wall scrapes) — summit-run's flat hairpins are taken clean at autopilot speeds; slip only accumulates on banked/sharp bends, and summit-run has neither at drift-lap pace. GLIDE's twisty identity therefore shows on serpents-tail, not summit-run. On halo-flats' banked sweepers GLIDE 289 ≈ tank 290 > std 240 (+20%, banking-induced slip, not GLIDE-specific domination; glide's drift lap is also 0.4s SLOWER than standard's clean lap).**
- **New-track medal sanity (browser autopilot, day free-play PB writes):** summit-run PB 31.17–31.29s vs author 32.5s → author medal reachable, ~4% INSIDE author time; halo-flats PB 19.875–19.95s (5/5 ring line) vs author 24.0s → reachable with ring mastery; no-ring headless 23.01s ≈ author band for humans. Track-select cards show author+gold+silver+bronze all earned at those PBs (qa 09/10).
- **Rotation hits raced headless (real finish path, 2 laps, full rival grids):** daily 20260927 = summit-run (HALCYON/VESPER/APEX) → all 4 finished, autopilot P3, 61.29s, dry ledger confirmed (bestTimeMs null); daily 20260928 = halo-flats (JUNO/VESPER/APEX) → all 4 finished, P3, 45.35s, dry. Weekly 2026W39 R2 (boost-fest) and 2026W44 R2 (rain-finals) = summit-run → all 4 finished, autopilot P2 both (mid/mid/pro grid).
- **APEX LEAGUE full 4-race headless cup run (autopilot, real cup path startCupRun→applyRaceResult→cupTrophy):** R1 summit dusk APEX 60.97 / VESPER 61.05 / VESUVIUS 61.14 / player P4 (2-lap 61.29 from the daily run → +0.3s to P1); R2 halo night same order (player +0.5s); R3 volt-alley APEX/VESPER/KESTREL P4; R4 grand-gauntlet rain **SOVEREIGN P1 61.15 / APEX P2 / VESPER P3 / player P4** — pack finishes spread ~0.2-0.3s (rubber band). Player 12×4 = 48 pts → rank P3 **bronze** (P3 on points despite P4×4 because SOVEREIGN only races R4: final APEX 93 / VESPER 69 / YOU 48 / VESUVIUS 30 / SOVEREIGN 25 / KESTREL 15). **Verdict: sane.** The all-pro grid holds P4 by +0.3–0.8s margins — inside one good lap's variance; gold path verified through the real points code (`test/tmp/p5-apex-points.ts`): 2 wins + 2 P2s = 86 pts vs APEX 86 → **gold on the wins tiebreak**, outright when SOVEREIGN takes R4 (APEX 79); 1 win + 3 P2s = silver. Champion defense behaves as designed: SOVEREIGN usually wins R4 on track while the cup goes on points.
- **Browser QA (muted `?mute=1`, playwright-core + system Chrome/swiftshader, `qa/v10-phase5/`, NOT committed):** free play — summit-run dusk start/mid-climb CLOSE-cam shots + finish, halo-flats **night rival race** threading ring #2 at 188 km/h with headlight cone (05), chase/close/hood cam suite on BOTH new tracks (c1–c3), medal rows on track cards with 4-star author PBs (09/10). APEX flow end-to-end through the REAL menu path — fresh profile locked card `LOCKED — UNLOCK GRAND GAUNTLET` (11), forced unlock via chain medals + GT trophy → `ENTER` (12), **R1 summit-run dusk raced in browser** (13–15: interstitial → all-pro grid → finished, run advanced nextRace 1, P4 +12 pts), seeded ledger → **R4 grand-gauntlet rain raced in browser: SOVEREIGN won on track (P1 1:05.950), player P4 +3.6s, cup GOLD on points 87** + `ACHIEVEMENT UNLOCKED — APEX CHAMPION` (17/18) — the champion-defense design working live. GLIDE — locked `BEAT SOVEREIGN` (20) → unlock toast (21) → selected, DRIFT bar 77% longest in garage (22) → **serpents-tail rival race won P1 → `glideWins=1` + GLIDE RIDER pop** (24); drift chip `DRIFT +194` at 95 km/h with smoke, maxDriftAmount 1.0 (23). RINGMASTER re-verified organic: real autopilot 5/5 ring lap → `ringPerfectLaps=1`, achievement done. Touch portrait 390×844 `?touch=1` summit-run — steer pads + BRAKE/GAS + top buttons all present (25/25b). Reduced-motion — settings flag → `rig.speedFovEnabled=false` in-race (26). Old-profile migration — v2.2-era save seeded in browser boots clean: PB/streak preserved, legacy `camera`→`cam`, v10 stats default-merged 0, weekly ledger merged, no forced onboarding (27); headless migration harness 19/19.
- **Perf:** rain-rivals census **112 draw calls** (≤220 budget), summit-run 54–77, halo-flats 50–54 (headless numbers post-auto-quality-downgrade; pre-downgrade summit 77 — real-GPU sessions stay high-quality and well under budget). Allocs gate PASS. **10-minute mixed soak** (rivals → knockout → traffic → daily → free-play summit, mode every 2 min): 20 samples, heap 25.5–36.1 MB bounded (no leak trend), calls 48–126, zero crashes/page errors.
- **Findings/fixes: zero app-code defects found.** All failures during QA were harness-side (fresh-profile PLAY launches the tutorial — v8 design; `buildGarage()` needed before `show('garage')`; `?variant=` override correctly outranks `def.variant`; free-play uses settings variant — day — while `def.variant` night/dusk applies to rival/knockout/cup, matching `resolveVariant` main.ts:991). Notes (not bugs): summit-run start line sits on the seam-approach grade so an idling car creeps forward until the d28 boost pad catches it (salt-flats idles perfectly still — physics-consistent); `/favicon.ico` 404 pre-exists v10 (no favicon link shipped); stray 404 for dev-only `/sw.js` is by design (PROD-only registration). Drift-mode summit-run finding recorded above (regime fact, not defect). Balance verdict: **no retuning shipped — GLIDE bands, new-track medals, and APEX difficulty all land inside their design intent.**


## Phase 6 (lead)
- v2.5.0 bumped; AGENTS.md updated (16 tracks, 5 cups, 4 bodies, 18 achievements, per-body harness).

## v10 final wrap (2026-09-15)
- All 6 phases complete; pushed `e182836..cc969b2`, deployed (deployment im4ovgzcjp6zfif0doacqarn, finished on cc969b25), **v2.5.0 live**.
- Live verification (muted): v2.5.0 + APEX LEAGUE + GLIDE strings in served bundle; sw controlling page; summit-run race live on prod with the crossover + ghost battle (`qa/v10-live/01`); halo-flats live (`02`).
- Gates at HEAD (20): typecheck ✅ build ✅ laps 16/16 ×4 bodies ✅ rivals 29/29 ✅ career 213/213 ✅ share 51/51 ✅ knockout 50/50 ✅ daily 59/59 ✅ probe 61/61 ✅ allocs PASS ✅ camera 28/28 ✅ ghostbattle 39/39 ✅ traffic 16/16 ✅ weekly 116/116 ✅ stats 100/100 ✅ tutorial 73/73 ✅ replay 34/34 ✅ variants ✅ draft 21/21 ✅ (+ migration repro 19/19).
